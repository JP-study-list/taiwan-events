# AGENTS.md — 外部代理在本 repo 的作業範圍

> 這份是**護欄**，不是專案說明。專案背景在 `CLAUDE.md`。
> 放在根目錄是因為要**自動載入**——寫在別處的規則等於「希望你會去讀」，那不叫護欄。

---

## ⚠️ 先讀這段：正常情況下你不應該在這裡

**2026-08-22 起，景點蒐集的代理在一個獨立資料夾工作，不進這個 repo**
（`~/Projects/kanto-places-codex`，由 `codex_kit.py` 與專案同步）。

**本檔是「萬一有代理被指到這裡來」的保險。** 如果你正在讀它而沒有人明確
交代你要在這個 repo 動手，**請先停下來問人**——你多半被指錯資料夾了。

真的要在這裡動手的話，範圍如下。

---

## 你在這個 repo 只做一件事

**蒐集常設景點資料，寫進 `places_src/` 的清單檔。** 其他全部不是你的工作。

開工第一件事：**讀 `codex_kit/_CODEX.md`**，那份講的是「查證到什麼程度才算完成」。
欄位怎麼填看 `places_src/_template.json`。

---

## 讀 vs 寫：讀全部都可以，寫只有一個地方

**讀**：整個 repo 隨你讀。你**應該**去讀 `places.json`（現有 105 筆，比對重複用）、
`build_places.py`（想知道某個欄位怎麼被處理時）、`project-index.md`（專案結構）。
讀不會弄壞任何東西。

**寫**：**只有 `places_src/` 底下非 `_` 開頭的 `.json`。**
按都道府縣分檔，關東現有這六個：

```
places_src/tokyo.json
places_src/kanagawa.json
places_src/saitama.json
places_src/chiba.json
places_src/kanto-north.json
places_src/shizuoka-yamanashi.json
```

**關東以外的地區（大阪、京都、福岡…）要收**，一個都道府縣開一個新檔
（`osaka.json`／`kyoto.json`…），放在同一層。那叫**預收**：座標照查、
資料照驗，只是暫時不進 `places.json`，等地區桶開通就會上線。
詳見 `codex_kit/_CODEX.md`。

**上面這個資料夾以外的每一個檔案都是唯讀的。** 白名單不是「這些比較重要」，
而是「除此之外一律不准動」——黑名單漏一項就破功，白名單漏一項只是你得回來問。

---

## 絕對不要碰（每一項都寫了後果，不是形式規則）

| 不要碰 | 為什麼 |
|---|---|
| **`events.json`** | 708KB 的**累積式**資料庫，不是可重生的暫存檔。活動的 `id` 是「第一次收錄時發下的號碼」，靠舊檔沿用；**舊檔沒了就沿用不到，而標題每天被 LLM 重新解讀，於是大量活動換號 → 使用者的收藏與分享出去的行程連結靜默失效**。而且來源當天抓取失敗時，舊資料是唯一備援 |
| **`places.json`** | **產出檔**。手改沒有意義（下次跑管線就被蓋掉），但改壞了會讓管線把錯的值繼承下去——`hours`／`holiday` 是從這個檔繼承的，不是每次重抓 |
| **`places_src/_geocache.json`**<br>**`restaurants_src/_geocache.json`** | 座標快取，**工作檔不是產出檔**。分別是 120 與 **3,318** 個地址。刪掉要重打国土地理院，而它限速 1 req/s——餐廳那份重查一次約 **55 分鐘** |
| **任何 `.py`** | 四支管線程式。要改的話是人來改 |
| **`js/` `css/` `index.html`** | 前端。與景點資料無關 |
| **`CLAUDE.md`** | 另一個代理的規範檔 |
| **`places/`** | 景點照片。使用者自己拍的，WebP 壓過回不去，原圖在外接碟 |
| **`restaurants/` `restaurants_src/`** | 餐廳資料，另一條完全獨立的管線 |

---

## 絕對不要執行

- **`git commit`／`git push`** —— push 到 `main` 會**直接觸發 Cloudflare 正式站部署**。
  你的產出要由人看過報表才決定上不上線。
- **`git add -A`／`git add .`** —— 這個 repo 明文禁止，要逐檔指定。
- **`git reset --hard`／`git rebase`／`git push -f`／`rm`** —— 不可逆。
- **`python3 build_places.py`（及其他三支管線）** —— 它會打国土地理院與 Overpass
  這些**有限速、會被擋 IP** 的外部服務，還會覆寫產出檔。**跑管線是人的工作。**
  想確認你的 JSON 格式對不對，用 `python3 -m json.tool <檔案>` 就好。

---

## 改既有清單檔的兩條規則

1. **先驗 round trip**：把檔案讀進來再原樣寫回去，確認**逐位元組相同**，再動內容。
   否則 diff 會混進一堆格式雜訊，人就看不出你到底改了什麼。
2. ⚠️ **`kanagawa.json` 與 `tokyo.json` 檔尾沒有換行，另外四個有。**
   寫回時沿用各檔原本的結尾，不然會多出兩行無關的 diff。

---

## 不確定的時候

**停下來問人，不要自己決定。**

這個專案踩過的坑，九成的症狀都是「看起來完全正常」——地址只到區級會查到區公所
而報表顯示「成功」、營業時間抓到隔壁咖啡廳的而畫面上毫無異狀、圖釘掛到 233 公尺外
的另一個景點而在地圖上分辨不出來。

所以**「我查到了」不算完成，「我確認過我查到的是對的東西」才算**。
確認不了的時候留空並說出來，比填一個看起來合理的值有價值得多。

回覆一律用**繁體中文**。
