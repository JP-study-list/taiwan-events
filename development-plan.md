# 寄道日和・台灣 — 開發計畫

> ⭐ **本專案唯一的待辦來源**（2026-09-23 初始化時建立）。
> `progress.md` 是歷史、會一筆一筆往下沉；**還沒做的事只寫在這裡**。
> `_ref-kanto/development-plan-v3/v4-kanto.md` 是日本版的計畫，**只作參考，不是本專案的待辦**
> （飯店可參考日本版單位 F、SEO 可參考單位 Z）。

---

## 0. 使用者的目標（2026-09-23 定案）

- 做**台灣版寄道日和**：活動・景點・飯店・法規全部是台灣的，介面中文＋日文。
- 站名 **「寄道日和・台灣」**，網址 **`events.tw.rensakobo.com`**（憑證未驗證，見單位 A）。
- ~~首發只做活動~~ → **2026-09-25 使用者改：先把大部分功能做完再上線**（範圍待定，H 景點先談）。景點、餐廳分頁目前仍藏著（D-4 的開關），做好再打開。
- 與日本版**完全獨立**；兩邊設定頁各放一個「切換到另一版」的**連結**（不共用網址）。
- **進行順序（2026-09-23 使用者調整）**：先在**公開 repo** 開發 → 設計結束後**拆成私有＋公開兩個 repo**
  → 私有那個再上 Cloudflare。**Cloudflare 相關（F、A）放最後。**
  ⚠️ 設計期間推上公開 repo 的程式，拆分之後依然是公開的（歷史收不回來）。

---

## 0-D. 工作單位總表

> **編號規則（沿用日本版，2026-09-23 起本檔從 A 重新開始）**：
> - 新待辦一律在這張表加一列、編下一個字母；**字母是編號不是優先序**。
> - A～Z 用完後接日文五十音（あいうえお／かきくけこ／…／わをん，不用濁音、半濁音、拗音），
>   `あ` 就是第 27 個單位。
> - 一個單位底下有多件**可以各自完成、各自驗收**的事時，編成 `X-1`／`X-2`。
> - **編號與子項編號一律不重複使用**，做完也不讓給下一件事。
> - 細節在 §7，一個單位一小節；這張表只出「有哪些、卡在哪」。

### ✅ 已完成

| 單位 | 是什麼 | 完成 |
|---|---|---|
| **C** | 工具去日本化防呆：C-1 `codex_kit.py`／C-2 `build_photos.py`（連同 `pick_photos.py assign`）拿掉指向日本的預設路徑，沒指定就停下來說明；C-3 User-Agent 改成 `taiwan-events-*`，聯絡網址用 GitHub repo | 2026-09-23 |
| **D-1** | 站名：中 `寄道日和・台灣`／日 `寄道日和・台湾`，副標沿用 `YORIMICHI BIYORI`；分頁標題「台灣展覽、祭典與活動地圖」 | 2026-09-23 |
| **D-3** | localStorage 前綴 `jpev_` → `twev_` | 2026-09-23 |
| **E-1** | 活動管線時區改 `Asia/Taipei`（變數 `JST` → `TW_TZ`） | 2026-09-24 |
| **E-2** | 活動 `id` 改用**中文標題**算（不退回 `title_ja`） | 2026-09-24 |
| **E-3** | 20 個地區桶＋五大區：後端地區表、前端選單／分組／行程大區／地圖預設視野、`test_areas.py`（查座標的日本機制留給 E-5） | 2026-09-24 |
| **E-4**（第一段） | 接文化部藝文活動開放資料：表演（音樂、戲劇、舞蹈、獨立音樂、演唱會）＋展覽，按場地拆巡迴、官方座標加距離守門、AI 批次翻日文（先沿用既有譯文）。實跑 1,205 筆、84% 精確座標。第二段（網站補缺）仍在未完成 | 2026-09-24 |
| **E-5** | 台灣版查座標：官方座標（距離守門＋地址交叉比對）→ Nominatim 結構化地址（帶區）→ Photon 場館名＋縣市 → AI 座標 → 區中心 → 縣市中心；名稱比對加中文通用詞、台／臺統一、兄弟設施規則；`GEO_VERSION` 12；新常設測試 `test_tw_geo.py`。實測 60 筆：精確 53、中位誤差 50m、錯 >5km 卻標精確 0 | 2026-09-24 |
| **E-4**（第二段） | **觀光署活動資料**（AI 分類八類或不收，分類存在 `events.json` 的 `tb_class`）＋**動漫網站 5 個**（萌朧、輕旅行、CWT、開拓 FF、台北國際動漫節）＋網站抽取加 `addr`＋**跨來源去重**＋場館名多種寫法查座標。Actions 實跑：1,423 筆、八類都有、去重合併 28、日文 100%、精確座標 97% | 2026-09-24 |
| **E-10** | 第八類「運動」：藍（#1875C0／圖釘 #6693CE／深色 #6BAEFE）＋跑步圖示，顏色用搜尋找出最大色相缺口 | 2026-09-24 |
| **K** | 法規查證與文案（⚠️ **非律師審閱**，依官方規定與常見做法）：①**政府開放資料顯名聲明**（授權條件，文化部與觀光署兩份，中日頁面都放）；②活動照片說明＋權利人移除申請（使用者選「繼續用售票網站宣傳圖」）；③隱私權外部服務清單依功能開關（加活動照片來源、景點關時不列 Commons）；④聯盟連結說明在景點關時用「沒有聯盟連結」版本；⑤公平會薦證廣告規範與日本ステマ規制：現有「廣告」標示已足（目前站上也沒有聯盟連結）。**景點上線（票券連結出現）時要再檢查一次** | 2026-09-25 |
| **E-8** | 文化部展覽場地修補 `moc_venue`：「鹿港鎮（彰化縣）=」→ 從演出／主辦單位挑**像場館、不像機關**的名字，並用 Photon 查證**不在別的鄉鎮區**（主辦的博物館常在別處辦展）才採用，本區查到更具體的名稱就用它（郵政博物館→郵政博物館高雄館）；否則清成乾淨的鄉鎮名。只寫館內空間 → 館名（空間）。只寫鄉鎮 34→24、「（縣）=」尾巴 0。剩 2 筆「第N展覽廳」（藝術教育館）資料裡沒有館名線索 | 2026-09-25 |
| **D-5** | 設定頁最下面加「日本版」入口卡（連結、開新分頁、外部連結圖示；中日文案）。瀏覽器中日 14 項全過。⚠️ **日本版那一側的「台灣版」連結要回日本版 repo 做**（本 repo 不改日本版） | 2026-09-24 |
| **D-6** | 介面文字換成台灣：資料來源、關於本站（中日）、「想跑遠一點」、網頁描述（原本在 D-2）；導覽「分幾塊」改成**跟著功能開關算**。瀏覽器中日 15 項全過、畫面上不再有日本地名。隱私權／廣告標示仍照日本法規，跟著 K | 2026-09-24 |
| **D-4** | 景點、餐廳整個藏起來：**開關只有一處**（`index.html` 的 `<html class="no-places no-restaurants">`），CSS 藏入口（分頁、餐廳收藏、加一頓飯、圖片出處）、`FEATURES` 讓地圖切換器只剩活動時整個不畫、不抓 `places.json`。瀏覽器 A/B（開關生效 vs 拿掉開關）各 9 項全過 | 2026-09-24 |
| **N** | **活動圖片**：①沒圖的活動去官方頁抓分享預覽圖（`fill_og_images`，誠實爬蟲 UA、每秒 1 頁、同一張圖被 ≥3 頁共用＝網站橫幅不採用、`og_tried` 14 天內不重抓），實測 60 筆補到 49（OPENTIX／年代售票）；②沒圖時改成淡色底＋分類圖示（使用者看對照圖選 C） | 2026-09-24 |
| **E-7** | 類型改七類：拿掉寶可夢、加表演（接手寶可夢色位、圖示音符）；祭典圖示由鳥居換成亭子；AI 分類規則換成台灣的例子；`test_areas.py` 加「前後端分類一致」 | 2026-09-24 |
| **E-6** | 給 AI 的指令：`title`／`venue` 照抄中文原文，`_ja` 欄位才翻成日文 | 2026-09-24 |
| **B-1** | 建**公開** repo `JP-study-list/taiwan-events`，從乾淨起點推上去（設計期間在這裡開發），見 §7-B | 2026-09-23 |

### ❌ 未完成

| 單位 | 子項 | 是什麼 | 卡在哪／要先做什麼 |
|---|---|---|---|
| **A** | — | **網址憑證實測**（`events.tw.rensakobo.com` 兩層子網域） | **排在最後**：要先有 Cloudflare Pages 專案（F）。⚠️ **網址是最不可逆的決定，上線前定案** |
| **B** | B-2 | 設計結束後拆分：私有 repo（程式＋資料）＋公開 runner repo（只放 workflow）；拆分時決定兩個的名稱 | 等設計結束 |
| | B-3 | 另申請一把只授權台灣版私有 repo 的 PAT | 跟著 B-2；日本版的 `KANTO_PAT` 只授權 kanto-events |
| **D** | D-2 | canonical／OG 網址（描述文字已在 D-6 改好）、`robots.txt`／`sitemap.xml`、`fetch_events.py` 檢查圖片時送的 `Referer` 改指新網址；UA 的聯絡網址視情況改成網站 | 網址定案（A） |
| | D-7 | 用 iPhone 實機確認主畫面名稱「寄道日和・台灣」（7 字）有沒有被截斷 | ⚠️ **上線前做**：一加到主畫面就寫死在使用者手機上。未實測 |
| **E** | E-4 | 第二段（觀光署＋動漫網站）**2026-09-24 已在 Actions 上實跑驗證**，見已完成的 E-4（第二段）。剩下的小尾巴：觀光署同一活動標題差幾個字時沒合併（「仙境西拉雅 環騎5COOL」與「…自行車系列活動」）；少數日文譯名用字不一致（衛武營／衛武営）、偶有標題沒翻 | 不急 |
| | E-9 | 座標複驗換成台灣版（日本版拿国土地理院反查縣別；台灣版目前 `REVERIFY_PER_RUN = 0` 停用）。可用 Nominatim reverse 取縣市 | 不急：官方座標已交叉比對、查詢有縣市守門 |
| | E-11 | 國字門牌（「南海路四十七號」）拆不出，座標只能退到區中心。實測文化部 1,209 筆只有 7 筆、其中 6 筆有官方座標 → 報酬太小先不做 | 不急 |
| **F** | — | Cloudflare Pages 專案、組建命令、runner workflow 改寫與 cron 時間。**圖片改用 Cloudflare 在亞洲縮圖**取代 images.weserv.nl（待辦 P；屆時先查當時的免費額度與設定方式）。⚠️ **cron 要避開深夜**：文化部 API 凌晨 2:40 實測只回部分資料（CLAUDE.md §8 地雷 8），挑時段時要實測 | **排在最後**：要先有 B-2 的私有 repo |
| **G** | — | 常設測試改成守台灣的規則。`test_areas.py` 已改（E-3，對餐廳 AREAS 暫時只提示，I 做完改回）；**新增 `test_tw_geo.py`（E-5）**；`test_geo_gates.py` 守的是日本版查座標（已不使用），跟 M 一起處理 | 跟著 M |
| **H** | — | 景點台灣化。**2026-09-25 使用者定調：不要整份觀光署資料（6,192 筆太多、雜），只收「最多網站或 KOL 都有推薦」的**。觀光署景點資料當底（每筆都有座標、介紹、照片），挑選方式另議（交叉比對多份推薦清單，出現次數多的才收）。資料實查：觀光署 `Attraction-json.zip`、遊客人數統計資料集 8116。**同日使用者再定：台灣與日本的推薦綜合考慮、台北可以多收、KOL 那票由 AI 整理。** 台北試做（2026-09-25）：台灣 4 份＋日本 5 份推薦文章各 1 票、2024 遊客破百萬＋1、維基 10 種語言以上＋1，滿分 11，4 分以上收（17 筆）、3 分邊緣（12 筆）。台北名單已定（32 筆，見 §7-H）；下一步擴到其他縣市 | 上線前（2026-09-25 改） |
| **I** | — | 餐廳台灣化 | 首發之後 |
| **J** | — | 飯店（全新功能） | 首發之後；日本版從未動工，無現成程式 |
| **P** | — | **圖片第一次載入慢**：✅ 2026-09-24 先修掉最冤枉的一段——代理抓不到的 17 個圖片網站由管線每輪實測、前端直接載原圖（第 3 秒載好 2→16 張）；內部主機名稱的圖片網址（`http://data-service/…`）直接清掉。**剩下**：代理（歐洲）第一次載入仍要 1～7 秒、海外預熱無效 → 上線時改 Cloudflare 縮圖（記在 F） | 跟著 F；⚠️ 日本版前端同一套，可能也有 |
| **O** | — | **AI 備援**：GitHub Models 已於 2026-07-30 停止服務（CLAUDE.md §8 地雷 11），供應商鏈只剩 Gemini；Groq 從未設定。要不要接第二家（例如申請 Groq 金鑰）。⚠️ 日本版管線應有同樣的問題 | 使用者決定 |
| **M** | — | 清掉日本版的死碼：寶可夢行事曆（`resolve_pokemon_calendar`／`POKEMON_*`）；**E-5 之後日本版查座標整套不再被呼叫**（`resolve_coords`／`geocode`／`geocode_google`／`fallback_center`／`PREF_CITIES`／`PREF_BY_CODE`／`reverse_pref`…）。`test_geo_gates.py` 守的就是這批，要一起處理（G） | 不急；刪之前先確認沒有呼叫點 |
| **L** | — | `AGENTS.md` 改寫；決定台灣版要不要接外部代理蒐集景點 | 跟著 H |

---

## 7. 各單位細節

### 7-A 網址憑證實測
Cloudflare 免費 Universal SSL 只蓋一層（`*.rensakobo.com`），`events.tw.rensakobo.com` 不在範圍內。
驗法：第一次部署時在 Pages 加這個自訂網域，看憑證會不會變有效、瀏覽器有沒有「不安全」。
- 會 → 照這個網址走。
- 不會 → 付費進階憑證，或改一層網址（例 `tw-events.rensakobo.com`）。

### 7-B 公開 repo 的乾淨起點（2026-09-23）
本機原本的歷史含有日本版私有 repo 的東西：`_ref-kanto/`（日本版的 CLAUDE.md、開發紀錄、計畫書），
以及 `fetch_events.py` 的 `SOURCES`（日本 138 個活動來源）、`SOURCE_PREF`、`SOURCE_CITY`。
使用者選擇「乾淨起點再公開」：
- 舊歷史改名成本機分支 `local-history`，**不推上去、不刪**。
- 新的 `main` 從一個全新的 commit 開始：`_ref-kanto/` 加進 `.gitignore` 只留本機，三份清單清空。
- ⚠️ `_ref-kanto/` 只在這台 mac 上；換電腦要自己從 kanto-events 複製。
- ⚠️ `fetch_events.py` 的**註解**裡仍散見日本來源的名稱（沒有網址），判斷不值得逐條清。

### 7-E 活動管線台灣化：已定案（使用者 2026-09-23）
- **地區**：縣市為桶＋上面一層五大區（北／中／南／東／離島），同日本版「圈＋桶」結構。
  ⚠️ 桶名上線後不能改（改名＝那一桶的既有活動下一輪被整批刪掉，日本版地雷 #16）。
- **類型**：沿用日本版，拿掉「寶可夢」，加「表演」（音樂、戲劇、舞蹈、演唱會併一類）。
- **來源**：開放資料為主＋網站補缺。開放資料直接轉換（地區從地址判斷、AI 只翻日文）；
  祭典、市集、煙火、燈會這類開放資料沒有的，挑網站用 AI 抽。
- **範圍**：首發全台灣。
- **20 個桶（⚠️ 名稱定案，上線後不可改）**：

  | 五大區 | 桶 |
  |---|---|
  | 北部 | 台北、新北、基隆、桃園、新竹、宜蘭 |
  | 中部 | 苗栗、台中、彰化、南投、雲林 |
  | 南部 | 嘉義、台南、高雄、屏東 |
  | 東部 | 花蓮、台東 |
  | 離島 | 澎湖、金門、馬祖 |

  - 宜蘭歸**北部**（國發會分區；雪隧到台北約一小時）。
  - 新竹、嘉義的市與縣**合併**成一桶。
  - 用字「**台**」不用「臺」；開放資料裡的「臺」由程式對應。
  - 離島**分開**三桶；馬祖＝連江縣。
- **七個類型**：展覽、表演、祭典、煙火、市集、動漫、點燈。
  「表演」**接手寶可夢的色位**（日本版八色是量過彼此分辨度才定的，換色位要重算）。
- 查證（2026-09-23 實打）：文化部藝文活動開放資料
  `cloud.culture.tw/frontsite/trans/SearchShowAction.do?method=doFindTypeJ&category=<類別>`
  免金鑰、JSON。當天筆數：音樂(1) 496／戲劇(2) 265／舞蹈(3) 73／親子(4) 68／展覽(6) 325／演唱會(17) 12。
  每筆有 `startDate`／`endDate`、`showInfo[].location`（完整地址，開頭是縣市）、
  約 1/3 的場次附 `latitude`／`longitude`。
  觀光署舊的 `media.taiwan.net.tw/XMLReleaseALL_public/activity_C_f.json` 已 404；
  新平台 TDX 要註冊拿金鑰，**未實測**。

### 7-E-4 第二段：觀光署資料與動漫網站（使用者 2026-09-24 定案）
- **觀光署活動資料**：`media.taiwan.net.tw/XMLReleaseAll_public/v2.0/Zh_tw/Event-json.zip`（data.gov.tw 資料集 7778，
  每日更新）。舊網址 `XMLReleaseALL_public/activity_C_f.json` 已 404。
  - 當天 1,088 筆、未結束 268 筆，**全部有座標、圖片、縣市與區**；只有 56 筆有活動網址。
  - 它的 `EventClasses` 對不上本站分類（代碼 2 裡混著市集、農遊、APP 活動），名稱關鍵字分類有 37% 分不出來
    → **AI 讀名稱＋簡介分類**，每筆只分一次、之後沿用。
  - **收**：七類＋運動賽事（新第八類「運動」，E-10）。**不收**：優惠／抽獎／乘車優惠這類不是活動的、導覽遊程與小旅行。
  - ⚠️ 45 筆共用同一組座標（例：5 筆不相干的活動都掛在桃園市政府，其中一筆在復興區）→ 同一點被 ≥3 筆共用就視為代表點、不採信。
- **動漫**：文化部與觀光署都沒有，這次一起查證網站、用 AI 抓。
- 臺北旅遊網的開放 API 被 Cloudflare 機器人驗證擋住（403），**不繞過**。

### 7-E-2 活動 id 用中文標題
日本版用日文標題算 id，因為那是原文；譯文每天被 LLM 重翻會漂移（日本版地雷 #8）。
台灣版原文是中文，照抄日本版算法等於把漂移帶進來。

### 7-H 景點：挑選方式與台北名單（使用者 2026-09-25 定案）

**方式**：每個縣市找台灣與日本的推薦文章（部落客、旅遊網站），每篇有列＝1 票；
觀光署 2024 遊客人數破百萬＋1（資料集 8116，只統計有計人數的據點，**只能加分不能當門檻**）；
維基百科 10 種語言以上＋1。台北滿分 11（台灣 4 篇＋日本 5 篇＋2），**3 分以上收**（台北可以多收），
再加使用者人工指定。**「很多人去但沒人推薦」的不收**（國父紀念館 2 分，使用者沒挑）。
座標、介紹、照片優先用觀光署景點資料（`Attraction-json.zip`，每筆都有）。

**台北市定案 32 筆**：
- 4 分以上（17）：台北101、大稻埕・迪化街、國立故宮博物院、艋舺龍山寺、中正紀念堂、北投溫泉・地熱谷、西門町、
  臺北市立動物園、士林夜市、陽明山、寧夏夜市、兒童新樂園、行天宮、貓空、剝皮寮、象山、國立臺灣科學教育館
- 3 分（12）：榕錦時光生活園區、國家鐵道博物館、臺北市立美術館、霞海城隍廟、民生社區・富錦街、永康街、
  饒河街夜市、天文科學教育館、國立臺灣博物館、華山1914文創園區、松山文創園區、台北當代藝術館
- 使用者人工加（3）：松山飛機巷、師大夜市、公館夜市

**還沒解決**：大稻埕、北投、陽明山、貓空是區域，要選代表點或拆點；國家鐵道博物館、榕錦時光、
松山飛機巷不在觀光署資料裡，座標照片要另補。試做頁（私人）：claude.ai/artifact/WZX6H6JHJk97rFvKDw4X3f

### 7-K 法規
現有廣告標示與隱私權文案是照日本法規寫的。Klook 同帳號（AID 133174）可用於台灣商品，
台灣版要一組**固定**的不同標籤（例 `tw-popup`）；⚠️ 標籤只能是少量固定值，絕不可一筆資料一個。

### 其他已知成本
- Cloudflare 建置額度每月 500 次、整個帳號共用（台灣版約多 30 次／月）。
- 私有 repo 的 Actions 額度帳號共用（每月 2000 分鐘）。
- Gemini 金鑰若與日本版共用，免費額度也共用，兩邊排程要錯開。
