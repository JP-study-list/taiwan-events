# CLAUDE.md — 開發規範與專案脈絡（單一檔）

> 本檔為通用規範 + 本專案技術背景的合併檔，放在專案根目錄，Claude Code 啟動時自動載入。
> 新 repo 只需複製本檔（§0~§7 通用部分照抄），第一次對 Claude Code 說「初始化這個專案」，
> 其餘檔案（project-index.md、progress.md）與 §8 專案背景會自動長出來（見 §1 自舉）。
>
> ⚠️ 本檔會 commit 上公開 repo。**嚴禁寫入任何 secret**（見 §4 規則 D 的可寫/不可寫清單）。

---

## 0. 語言與溝通
- 一律使用**繁體中文**回覆。
- 解釋精簡、切中要點，節省 token。

---

## 1. 啟動 SOP（每次新 session 開場）

### 一般啟動
1. 讀 `project-index.md`（全部）——掌握結構與各檔用途，**不重掃全部原始碼**。
2. 讀 `progress.md` **最新 3~5 筆**——掌握上次進度與待辦。
3. 需動到某檔時，才針對性讀該檔。

### 首次進專案 / 使用者說「初始化這個專案」時（自舉）
依序執行，**全程只新增、不刪除、不覆蓋**：
1. **讀**現有全部檔案（唯讀，不動任何一個）。
2. 若 `project-index.md` **不存在** → 自動生成；若已存在 → 更新，**絕不覆蓋既有內容**。
3. 若 `progress.md` **不存在** → 建立空模板；若已存在 → 保留。
4. 訪談使用者補齊 §8 專案技術背景，寫進本檔 §8 區塊。
   - **只問非機密項**（Project ID、collection 結構、部署 branch 等）。
   - 遇到金鑰類（`/exec` URL、API key）→ **不寫進本檔**，改提醒使用者放 `.env`，本檔只記指標。

> **自舉硬規則**：初始化流程明文禁止任何 `rm`、覆寫、`git reset` 等破壞既有檔案的操作。只建立不存在的檔。

---

## 2. 開發流程規範（嚴格執行）

收到任何工具／功能開發請求時，**不得立即寫程式碼**。依序：

### Phase 1：需求確認（必做）
1. 用自己的話覆述需求。
2. 提釐清問題，涵蓋：使用情境（手機／桌機／網頁、頻率）、核心功能（MVP vs. 加分）、UI 偏好（語言預設繁中、深/淺色、版面）、資料處理（需持久化？存哪？）、技術限制（單一 HTML？React？GitHub Pages？）。
3. 一次最多 3~5 個關鍵問題，不洗版。

### Phase 2：提案（必做）
- 功能清單（MVP vs. 未來擴充）
- 技術方案 + 一句話理由
- UI 結構文字描述（免圖）
- 結尾問：「確認後才動工，有要調整的嗎？」

### Phase 3：實作
- 僅在明確說「start／確認／go ahead」後開始寫碼。
- 驗證與 review 嚴謹徹底；交付前自我檢查功能完整性。

### 例外
- 瑣碎請求（一行 CSS、明顯 bug）可跳過，但先說「Simple request, proceeding directly」。

### 中途變更
- 需求中途更動且影響架構 → **先指出影響範圍**再動手。

---

## 3. 交付原則（本機環境）
- 直接讀寫本機檔案，不再提供「完整檔案手動貼回／ZIP」。
- 偏好乾淨美學：衝突時，簡潔 > 附加功能；果斷、直接。
- 期望根因診斷，不要表面修復。

---

## 4. 本機環境安全與協作規則（ABCD）

### A. Commit 與 push 規範
- **允許直接 commit**，不需事前確認。
- 訊息格式：`feat:` / `fix:` / `refactor:` / `docs:` / `chore:` 前綴 + 繁中摘要。
  - 例：`fix: 修正背景卡 shiny 切換未同步 Firestore`
- **push 分兩種情況**（2026-07-29 使用者確立）：
  - **純畫面／純功能的改動 → 本機驗證通過後直接 push，不必再問。** 例：前端樣式、新按鈕、介面文字、抓取邏輯調整。這類改壞了 revert 即可，來回確認只是拖慢節奏。
  - **會動到活動資料的改動 → 先說明影響範圍並等明確同意。** 判準是「推上去之後，下一次自動執行會不會讓既有資料消失或失效」，例：`SCHEMA_VERSION` 變更（整批汰換）、`id` 算法變更（收藏失效、活動重複）、直接刪改 `events.json`。這類不可逆，且 cron 一到就生效，所以 push 的時間點本身就是決策。

> 註：判斷不了屬於哪一種時，一律當成後者先問。

### A-2. 本檔的工作記錄義務
每次改動完成後，除了 commit，還要同步更新 `progress.md`（做了什麼、為什麼、怎麼驗證的、留下什麼待辦）與 `project-index.md`（結構有變動時）。踩到新地雷就補進 §8。

### B. 破壞性操作前先確認（最高優先，不因 A 而放寬）
- 以下操作**必須先說明影響範圍並等待明確同意**：
  - 刪除檔案、`rm`
  - `git reset --hard`、`git push -f`、`git rebase`
  - 大範圍重構、跨多檔結構性變更
- Commit 可逆，故 A 放行；上述不可逆，故一律先問。不確定是否具破壞性時，先問。

### C. 本機測試優先於宣稱完成
- 宣稱「完成」前，能本機驗證的先驗證：起 server、看 console、跑既有測試。
- 不憑「讀過碼看起來對」就宣稱完成。

### D. Secrets 不進 git（含本 CLAUDE.md）
本檔會上公開 repo，界線如下：

**可寫進本檔（非機密）：**
- Firebase **Project ID**（本就出現在前端 config，非機密）
- Firestore collection 結構、key 設計原則
- 部署 branch、部署平台與網域
- 使用的 API、資料來源、cron 時間
- 已知地雷

**絕不寫進本檔（外置到 `.env` / `config.local.js`，並列入 `.gitignore`）：**
- Apps Script `/exec` URL（等同後端入口，視為機密）
- 任何 API key / token / 私鑰、service account 憑證

本檔只記**指標**，例：`Apps Script /exec URL 存於 .env 的 APPS_SCRIPT_URL（不進 git）`。

> 註：Firebase 前端 `apiKey` 並非密鑰，本就暴露於客戶端，靠 Firestore Security Rules 保護；重點是 Rules 有沒有寫好，而非藏 key。Apps Script `/exec` URL 則須當機密。

---

## 5. 檔案維護機制

### progress.md — 開發歷史（每次改檔即更新）
- 反向時間序（最新在上）。
- 欄位：
  ```
  ## YYYY-MM-DD
  - 類型：新增 / 修正 / 重構
  - 影響檔案：xxx.html, yyy.js
  - 摘要：做了什麼
  - 原因：為什麼
  - 待辦/已知問題：（可留空）
  ```
- 小改允許精簡：只填「日期 + 類型 + 摘要」。

### development-plan.md — ⭐ **唯一的待辦來源**（2026-09-23 初始化時建立）
- **§0-D 是工作單位總表**，**§7 是各單位細節**。編號**從 A 重新開始**，與日本版的單位字母無關
  （本檔 §6b 教訓裡出現的「單位 き-1」「單位 Z-1」等指的是**日本版**的單位）。
- ⚠️⚠️ **新的待辦一律在 §0-D 加一列並編下一個編號。**
  **A～Z 用完之後接日文五十音，從 `あ` 開始**
  （あいうえお／かきくけこ／…／わをん，共 46 個，**不用濁音、半濁音、拗音**）。
  **五十音是同一個序列的延續，不是另一套**——`あ` 就是第 27 個單位。
  **編號不重複使用**，單位做完也不把字母讓給下一件事。
  ⚠️ **一個單位底下有多件獨立的事時，編成 `Y-1`／`Y-2`**（帶連字號，2026-09-03 使用者要求）。
  判準是**「這一件可以自己完成、自己驗收」**——不是「同一天做的」。
  **子項也不重複使用編號。**
  **只寫進 `progress.md` 的待辦行等於沒有寫**——progress.md 是歷史，一筆一筆往下沉，
  三十筆之後沒有人會往回翻。日本版 2026-08-28 使用者親口交代的「行程頁大區分層」就是這樣
  **消失了四天**，直到使用者自己問「之前有跟你討論過，但現在沒有看到」。
  ⚠️ **這與「口頭講的沒寫進 repo 就等於沒講過」是同一條的下一層**：
  寫進 repo 還不夠，**要寫進「還沒做的事」那份清單裡**。
- `_ref-kanto/development-plan-v3/v4-kanto.md` 是**日本版**的計畫，只作參考、**不是本專案的待辦來源**。

### project-index.md — 專案檔案索引（首次建立，每次改檔同步）
- 每個檔案的用途、彼此關係、進入點。
- 檔案新增／刪除／職責變動 → 同步更新。

---

## 6. 技術教訓（跨專案通用原則）

> 以下為既往踩坑固化的原則。**若當下發現更優解，先提出與使用者討論，不擅自沿用舊規則、也不默默改掉。**

- **Firestore key 設計**：用穩定 ID（`p###`、costume ID）當 key，seed 變動不破壞既有紀錄；只存狀態，不存顯示資料。
- **Apps Script 部署**：一律**編輯現有 deployment**（鉛筆 → 新版本 → 部署）保留 `/exec` URL，絕不新建 deployment。
- **Base64 圖片**：存 Firestore <700KB；iOS Shortcut Base64 encode 關閉換行。
- **GitHub Actions cron**：避開 UTC 午夜，偏移如 `43 0 * * *`。
- **日期／時區**：Taiwan UTC+8，日期邏輯用 local time 格式化，**不用 `toISOString()`**。
- **Google Maps API**：tile 依 ToS 不可快取；離線資料由 Firestore `persistentLocalCache` 處理。
- **靜態站資料源**：AniList GraphQL 支援 CORS 免金鑰；Nominatim 1 req/sec、免金鑰。
- **iOS Shortcut 分享**：LINE Flex Message 擷取走「截圖 → OCR」最可靠。
- **fast-flights**：用 `FlightQuery` / `create_query`（v3.x）；`FlightData` 為破壞性移除。
- **圖片格式**：背景卡等圖片 jpg/png/webp 混雜，寫死檔名前**先確認格式**。

---

## 6b. 本機驗證環境（**兩台電腦來回切換，先認平台再動手**）

> 使用者在 **mac 與 Windows 兩台電腦**上輪流開發同一個專案（2026-08-12 起）。
> 兩邊的指令差異大到會直接讓驗證失敗，而且**失敗長得像產品壞掉**。
>
> **開場第一件事：看系統提示的 `Platform`**（`darwin` = mac，`win32` = Windows），
> 只照對應那一欄做。**不要把另一個平台的指令套過來**——`py` 在 mac 不存在，
> `Start-Process` 在 mac 不存在，跑出來的錯誤訊息完全不會告訴你是平台選錯了。

### 平台對照表（**兩欄都是實測值，不是推測**）

| 要做的事 | mac（2026-08-12 實測） | Windows（2026-07-31 實測） |
|---|---|---|
| 專案路徑 | `/Users/rensa/Projects/taiwan-events`（2026-09-23 確認） | ⚠️ **台灣版尚未在 Windows 建立**（日本版是 `c:\kanto-events`）；第一次在那台開時實查並填回這格 |
| **跑 Python** | `python3`（3.14.6，走 Homebrew） | **`py`（3.10.5）**。`python` 是 3.7.3，**跑不動 `fetch_events.py`**（需要 3.9+ 的 `zoneinfo`） |
| 起本機 server | ⚠️ **本專案用 8931 埠＋下方那行指令**（2026-09-23，理由見表下兩條） | 日本版是 `python -m http.server 8899`；台灣版在 Windows 尚未實測 |
| **起 Chrome headless** | **Bash 工具直接起就行**，執行檔在 `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` | **Bash 工具下起不來**（exit 21，無 stderr），要改用 **PowerShell 的 `Start-Process`** |
| 關掉自己起的 Chrome | `pkill -f "<user-data-dir 的標籤>"` | 比對 CommandLine 找出含該標籤的程序 |
| 抓線上檔案 | `curl` 或 `node -e` + `fetch` 皆可 | **只能用 `node -e` + `fetch`**，見下方編碼坑 |

- ⚠️⚠️ **mac 上 `git` 可能被 Xcode 授權條款整個擋死**（2026-09-16 實測）。症狀是**任何**
  git 指令都回 `You have not agreed to the Xcode license agreements`、exit code 69，
  **連 `git status` 都不行**。根因是 `/usr/bin/git` 只是個轉發器，會轉給
  `xcode-select -p` 指到的那份（實測指向 `/Applications/Xcode.app/...`）。
  ⚠️ **不要代跑 `sudo xcodebuild -license`**：那要人互動同意授權，是使用者的決定。
  **繞法是每個 git 指令前面加一行**（不必 sudo、不改系統設定）：
  ```
  export DEVELOPER_DIR=/Library/Developer/CommandLineTools
  ```
  ⚠️ **Shell 狀態不會跨 Bash 呼叫保留**，所以**每一次**要用 git 的呼叫都要自己帶上它。
  ⚠️ **這個坑最貴的地方是它長得像「repo 壞了」**——第一次撞到時我差點以為是 git 目錄有問題。
  📌 mac 上**沒有** Homebrew 的 git（`/opt/homebrew/bin/git` 不存在），
  能用的那份在 `/Library/Developer/CommandLineTools/usr/bin/git`（實測 2.50.1）。
- **`--user-data-dir` 一律指到 scratchpad 底下並帶專案標籤**（例如 `chrome-kanto-test`）。
  關的時候**只比對這個標籤**，不要用程序名或 PID 差集去殺——曾因此把使用者正在用的 Chrome 全部關掉。
- **測完記得把 Chrome 與 server 都收掉。**
- ⚠️⚠️ **本機測試的埠要用本專案專用的：server `8931`、Chrome 除錯埠 `9241`**（2026-09-23 台灣版 D-1／D-3 踩到）。
  這台 mac 上**別的專案也在用 8899**，而 `python3 -m http.server` 綁的是 IPv6 的 `*`，
  別人綁 IPv4 的 `127.0.0.1:8899` 照樣綁得上，**兩個伺服器同時存在、瀏覽器打 127.0.0.1 會被導到對方那邊**。
  症狀是 localStorage 裡冒出別的專案的鍵（當時是 `poke-change/v1`）、語言讀到 `en`，
  **五項斷言失敗、而且看起來完全像產品壞了**。一律**明確綁 `127.0.0.1`**：被佔用時會直接報錯，不會悄悄共用。
- ⚠️⚠️ **`python3 -m http.server` 會隨機讓頁面 JS 整個沒跑**（2026-09-23 同一輪）。它的連線等候佇列只有 5，
  本站一次載入約 30 個 ES module，偶發 `ERR_CONNECTION_RESET`，某個模組沒到＝整頁 JS 不跑，
  `lang` 停在 HTML 寫死的 `zh-Hant`。**約 1/8 的載入會中**，所以「單獨重跑就過」。
  分辨方法是接 `Network.loadingFailed` 看有沒有被重設的請求。**起 server 用這行（在專案根目錄）**：
  ```
  python3 -c "import http.server as h,functools as f;S=type('S',(h.ThreadingHTTPServer,),{'request_queue_size':128});S(('127.0.0.1',8931),f.partial(h.SimpleHTTPRequestHandler,directory='.')).serve_forever()"
  ```
  收掉用 `pkill -f "request_queue_size':128"`。
- 📌 **篩選面板裡的地區筆數，要打開面板之後才讀**（2026-09-24 E-3）。開頁那一次 `buildAreaSel` 畫在資料到之前，
  筆數全是 0；`openFilter` 才重算。沒開就讀，會得到「每個桶 0 筆」而篩選照常正確——看起來像筆數壞了。
- 📌 同一輪還有第三個：**`load()` 等 300ms 就開始檢查，讀到的是「上一頁」**。導航前先在舊頁設
  `window.__stale=1`，等它消失才算新頁到了。

### 不分平台都成立的事

- ⚠️ **要模擬「沒有滑鼠的裝置」只有一條路：`Emulation.setTouchEmulationEnabled`**（2026-08-27）。
  **`hover` 這個 media feature 完全不吃 `Emulation.setEmulatedMedia`**——送 `{name:'hover',value:'none'}`
  進去毫無反應，`matchMedia('(hover:hover)')` 照樣回 true，`setDeviceMetricsOverride` 的
  `mobile:true` 也沒有用。實測只有 `setTouchEmulationEnabled({enabled:true,maxTouchPoints:5})`
  會讓 `(hover:none)` 與 `(pointer:coarse)` 一起成立（那就是 DevTools 的 device toolbar 在做的事），
  而且**導航之後會保持**。⚠️ **這件事的失敗方式極具說服力**：手機那條路的十項斷言會一起 FAIL
  （位置不對、點了不收、捲動不收），看起來就像整條路沒做，**但跑的其實一直是桌機那條**。
  **凡是靠 media query 分兩條路的功能，第一項斷言要先驗「環境真的被判成那一種」。**
- ⚠️ **一次性的 Chrome（`--screenshot`／`--dump-dom`）做完事不會自己結束**（2026-08-28 在 mac 上實測）。
  它會**把檔案寫好、然後掛在那裡**，於是前景執行必定撞上工具的 timeout，看起來像「Chrome 起不來」
  ——**但產出其實已經好了**。三件事一起記：
  - **一律用 `run_in_background` 起，再去看檔案在不在**，不要在前景等它。
  - **要加 `--headless=new`**：舊的 `--headless` 這次連截圖都生不出來。
  - ⚠️ **被 timeout 砍掉的那次會在 profile 裡留下 `SingletonLock`**，下一次啟動直接
    `Failed to create ... SingletonLock: File exists` 然後 abort。症狀是**第二次之後永遠失敗，
    而錯誤訊息只在 stderr**（我把 stderr 丟掉，於是看到的是「輸出空的」）。
    收拾方法是 `pkill -f "<user-data-dir 的標籤>"` 之後**整個 profile 資料夾砍掉重建**
    （`rm -f Singleton*` 在 zsh 沒有 glob 命中時會報 no matches 並中斷整串指令）。
  - 附帶一條：**`--dump-dom` 把 DOM 印到 stdout**，所以「頁面自己跑完 JS 把結果寫進 `<pre>`」
    是一條不必接 DevTools Protocol 的輕量驗證路徑（這次前端 12 項就是這樣驗的）。
  - ⚠️⚠️ **但 `--dump-dom` 不保證可靠，2026-09-16（單位 Z-1）它完全吐不出東西**：
    輸出檔一直是 0 bytes，背景跑、前景跑、加不加 `--virtual-time-budget` 都一樣
    （Chrome 152）。**那時不要在它身上耗時間，改走除錯埠**：
    `--remote-debugging-port=9222` 起 Chrome，**光是打 `http://127.0.0.1:9222/json/list`
    就拿得到每個分頁的 `title`**（純 HTTP，連 WebSocket 都不必接）——驗「標題有沒有被 JS 換掉」
    這種事一行就夠。要問更複雜的再用 Node 內建的 `WebSocket` 送 `Runtime.evaluate`。
    ⚠️ **等標題時要等它「連續數次不變」**再取，JS 是在載入之後才換的。
- **驅動瀏覽器做端對端驗證**：Node（mac 上為 v26）有**內建 `WebSocket`**，可直接寫 DevTools Protocol
  腳本，不需要 puppeteer。用 `--remote-debugging-port=9222` 起 Chrome，再從
  `http://127.0.0.1:9222/json/list` 取 `webSocketDebuggerUrl`。可做：點按鈕、讀 computed style、
  截圖、收集 JS 錯誤、`Emulation.setDeviceMetricsOverride` 換裝置尺寸。
- ⚠️ **腳本一開始就要送 `Network.setCacheDisabled`**。Chrome 對 **ES Modules 快取得非常兇**，
  改完 `js/` 底下的檔重新載入仍會拿到舊版，症狀是「明明修好了卻還在報同一個錯」。
  2026-08-12 為此白追一輪：`util.js` 補上 export 後仍報 `does not provide an export named`，
  但 `curl` 拿到的檔案明明是新的——**檔案對、瀏覽器錯**。
  分辨方法就是那一步：**先 `curl` 看 server 送出的內容，再決定要不要懷疑程式。**
  ⚠️⚠️ **2026-09-09 又踩一次，而這次連 `Network.setCacheDisabled` 都擋不住**（單位 き-2）：
    `store.js` 加了兩個 export 之後瀏覽器仍報 `does not provide an export named`，
    而 `curl` 拿到的明明是新版。原因是**那個 Chrome 實例是上一輪驗證時就起著的，
    而 module map 是 per-document 的、不吃 HTTP 快取設定**。
    **改了 `js/` 底下的檔又要重跑測試時，整個重啟 Chrome（連 profile 一起砍）最省事。**
    📌 **連帶一條，做 A/B 換版本時尤其致命**：不重啟的話兩邊跑的可能是同一份程式，
    **而那正是「改回舊寫法竟然也通過」的成因**（2026-08-21 踩過）。
- **`gh` CLI 在 mac 上「有」，在 Windows 上沒有**（2026-08-12 更正，原記載一律寫「沒有」）。
  mac 是 `/opt/homebrew/bin/gh`，已登入 `JP-study-list`。**這改變了很多事**：
  兩個 repo 的 workflow 內容、執行紀錄、log、secrets 清單都查得到，也能直接
  `gh workflow run` 觸發、`gh run watch` 看結果，不必再開瀏覽器貼 YAML。
  - 實例：2026-08-12 靠它一次查清「runner 只有 `update.yml`」「cron 確實在跑、每次 24〜30 分」
    「舊 repo 的 Update Events 已是 `disabled_manually`」——這三件事先前都只能猜，
    而文件裡有兩件記載錯誤。**動 Actions 相關的判斷前，先用 `gh` 查實況，不要讀文件推測。**
  - **Windows 那台仍然沒有**，在那邊要判斷某來源是抓取失敗還是抽取不到，
    只能自己用 Jina 重打那個網址實測。
- **Windows 專屬但後果嚴重，換到 mac 也要記得它存在**：查線上檔案不要用 PowerShell 的
  `Invoke-WebRequest`，它不依 `charset` 用 UTF-8 解碼，中文會碎成多個字元，於是
  `.Contains("北關東")` 一律回 `False`、`.Length` 膨脹約 1.5 倍（實測 8,757 → 13,779）。
  **這會直接被誤判成「部署失敗、線上還是舊版」**，2026-08-05 就差點據此下錯結論。
  同一個原因也會讓 `ConvertFrom-Json` 後的中文欄位變亂碼。

- **驗證失敗時，先懷疑測試、再懷疑產品。** 本專案已累計**四十次以上**「測試錯了但看起來像產品壞了」。
  ⚠️⚠️ **不要再編號，也不要靠 `grep` 找「最大的那個號」推下一號**（2026-09-09 確立）：
  **那個序列早就不連續了**——同一個「第四十八次」在 `progress.md` 裡被用過兩次，
  而 2026-09-02 那筆已經發現並留了註「這個計數自己對不起來，所以這筆不編號，
  免得把錯的數字傳下去」。**本輪沒讀到那句就又編了兩個號，然後才發現撞號。**
  **要引用某一次，用日期與單位指它**（例如「2026-09-09 き-1 那次」），不要用序號。
  **2026-09-11（單位 く）一輪錯了五次，其中三次長得像產品壞掉，而且有一次是「通過但什麼都沒測到」**：
  ①⚠️⚠️ **腳本兩次無聲卡死**，看起來像「頁面載不起來」——根因是**導航之後立刻輪詢，
  那一發打在正在被換掉的執行環境上，回應永遠不會回來**，而 CDP helper 沒有逾時。
  **寫 CDP 腳本時每一發都要有逾時，`waitFor` 要吞掉導航中的例外。**
  ⚠️ 同一件事還有一半：**輸出接 `tail` 會被緩衝**，所以「什麼都沒印」是看不到而不是沒跑
  ——長時間的腳本一律**寫檔＋背景執行**再去讀那個檔。
  ②⚠️⚠️ **「三種圖釘都在」在全日本尺度下必然是 0**（全部被叢集），於是三項連環 FAIL
  而產品完全正確。**凡是要驗「畫面上有沒有某個東西」，第一件事是確認視野裡真的該有它。**
  ③⚠️⚠️ **叢集重疊那一項原本沒有鑑別力，是突變測試才抓出來的**：它在拉近之後才量，
  而重疊只發生在遠景——**把程式改回「三組各自叢集」，它照樣 PASS**。
  📌 **凡是「不會互相蓋住／不會擠在一起」那一類斷言，先問它在哪個縮放層級才有東西可蓋。**
  ④**三種在同一個視窗裡湊不齊**（上野有景點沒榜單餐廳、澀谷相反），硬要全到齊只會讓斷言
  在 0 對 0 的情況下**通過**。改成逐層縮放驗不變式＋三種各自至少出現過一次。
  ⑤**我自己留著設定彈窗沒關**（切深色那一步），它 z-index 1150 整片蓋住地圖，
  於是「點餐廳圖釘」失敗成「視窗內找不到可點的圖釘」——**完全像產品壞了**。
  靠 `elementFromPoint` 回報 `sheet-box` 才分辨出來。**共用的測試步驟會留下狀態，
  每一步都要收乾淨。**
  以下為既往：
  **2026-09-09（き-1）拿篩選彈窗的「完成」鈕問「點不點得到」，得到 `none`**
  ——它在彈窗自己的捲動區最底下，**`top=985.2` 而視窗只有 844**，`elementFromPoint`
  對畫面外的點回 null 是**正確行為**，不是彈窗被蓋住（同單位 K 的「點到畫面外」）。
  改問彈窗頂端那個一定看得見的標題就過了。**凡是用 `elementFromPoint` 的斷言，
  同一句話裡就要一起斷言「它在畫面內」**（`r.top>=0 && r.bottom<=innerHeight`），
  否則「被蓋住」與「在畫面外」長得一模一樣。
  **同一輪還有一次（用語統一那一段）**：圖例寬度的上限**照中文訂成 100px**，
  日文三張全部 122〜124 判 FAIL——**A/B 量改動前是 143／122／122，活動那張其實窄了 19px**，
  而寬度是由常駐那行「おおよその位置」決定的、不是 summary。
  ⚠️ **跨語言的版面上限不可以只訂一個數字**，而且要把「改動前是多少」寫進註解。
  以下為既往：
  **2026-08-29（座標守門）第三十九次，而它是一種新的長相：測試「通過」但什麼都沒測到。**
  要驗「`GEO_CACHE` 有沒有分地區桶」，第一版的做法是**預先塞一筆
  `('宝徳寺', ('東京都',))` 進快取**，再斷言另一個桶拿不到它。
  在改動後的版本上 PASS，**在改動前的版本上也 PASS**——因為**舊版的鍵是字串，
  塞 tuple 進去它一輪都不會命中**，於是它會真的去打網路、拿回東京那個答案，
  斷言照樣成立。**測試沒有鑑別力，而且偷打了網路。**
  改成把三支查詢函式換掉、依 `allowed_prefs` 回不同答案，「有沒有分桶」才成為唯一的變因
  （改動前 13 FAIL、改動後 30 PASS）。
  ⚠️ **通則：造假資料去戳一個「鍵的形狀」時，先確認那筆假資料在對照組裡真的會被命中**
  ——否則對照組走的是完全不同的一條路，而**兩邊都會給你想看的答案**。
  同一輪的第 37、38 次見 progress.md 第一百四十五筆（`osm_pick` 的 `addr` 傳空字串、
  `--dry-run` 不寫報表所以讀到昨天的）。
  **2026-08-27（行程列照片預覽）第三十三～三十六次**：
  ①**最嚴重的一次，手機那條路十項連環 FAIL 而產品完全正確**——`Emulation.setEmulatedMedia`
  模擬不了 `hover`（見 §6b 上方那條），跑的其實一直是桌機那條路。
  ②**「唯讀模式下沒有刪除鈕」判 FAIL**：按鈕**在 DOM 裡**，是被 `.planview.ro .plan-editonly{display:none}`
  藏起來的，我的斷言假設它不輸出。**改斷言前先看一眼 computed style。**
  ③**「停下來才真的去載」量到 0 個請求**——那正是設計目的（預覽沿用清單卡片的同一個網址、
  命中快取），**最好的結果被寫成了失敗條件**。靠「比對兩邊網址是否逐字元相同」才分清楚。
  ④**測試自己把要測的情境抹平了**：輔助函式裡有一行 `scrollIntoView({block:'center'})`，
  於是不管先前捲到哪裡，點下去的 y 永遠在中線附近，「點畫面上半部」這個情境**再也造不出來**
  ——而畫面上看起來只是「那個 class 沒有生效」。**輔助函式裡的每一行都會影響情境，
  別在共用的 helper 裡偷偷捲動。** 同輪另有兩次純腳本錯誤：用 `nth-of-type` 猜列的順序
  （`orderStops`／`weaveFood` 會重排，午餐插中段），以及量元素寬度時忘了容器有 1px 邊框。
  以下為既往：
  **2026-08-27（月曆區間選擇）第二十九～三十二次，四個裡有三個是「日期不在那一頁上」**：
  ①`#calHint` 在改動前的版本不存在（A/B 的共用 snap 直接丟 TypeError）
  ②**今天是 8/27、八月的月曆只剩 5 格**，測試卻拿九月的日期去點
  ③**月曆開在「行程起日」那個月**（種子是 9/1），我又按了一次「下個月」跑到十月；
  同一支後段還踩到**切天會把 `calYM` 重設回起日那個月**（既有行為）
  ④**最像產品壞掉的那一個**：斷言「高亮回到兩端」拿到只有一端判 FAIL——但月曆當時停在九月，
  而行程是 **8/30–9/2**，**起日那一格根本不在這一頁上**，產品是對的。
  **分辨方法是把「現在是哪個月」一起印出來**：只看 `on` 的話，「少了一端」與「漏畫一端」
  長得一模一樣。同一輪還再次憑印象猜選擇器（`.tour-h` 實際是 `#tourH`），§6b 那條又踩一次。
  **2026-08-27（活動圖例收合）第二十七、二十八次**：斷言用 `/[展祭煙市]/` 判「還有中文」，
  但**日文的「展覧会」「祭り」本來就含這些字**，產品是對的；以及分頁鈕**裡面只有 svg
  一個字都沒有**（id 是 `tabRestaurant`／`tabPlaces`），憑 textContent 找當場丟 TypeError。
  **2026-08-27（景點圖例收合）第二十六次：`Page.addScriptToEvaluateOnNewDocument` 會疊加。**
  想測「只有一個小類」與「有 approx」兩種資料形狀，做法是注入一段包住 `fetch` 的腳本改資料——
  但**第二次注入時第一次的還在**，於是資料被過濾兩次（先只剩神社寺廟、再取前 40 筆），
  量到的是「這個資料集只有一個小類」，**而那個結果完全合理、看起來就像分支沒生效**。
  要用 `Page.removeScriptToEvaluateOnNewDocument` 收掉前一個（`add` 會回一個 identifier）。
  同一輪還再次踩到**起始明度不固定**（第四次）：強制 `jpev_theme` 之前拍的「淺色」截圖其實是深色。
  以下為既往：
  **2026-08-27（單位 K）一次踩到三個，全部長得像「這顆鈕沒接上」**：
  **①點得太早**——餐廳頁載完之後還會再 render 一次（`removeLayer` → `addLayers`），
  那一下會把剛開的彈窗關掉，**實測彈窗只活 300ms**；做法是等「標記數連續 1.4 秒不變」再動手。
  **②又點到叢集**（第十八次那條換一個頁面）：地圖穩定後那 21 個標記**全部是叢集**，
  而**被叢集起來的 marker 根本不在 map 上**，`map.eachLayer` 也看不到它
  ——要跟叢集群組要（`grp.getLayers()`）再用 `zoomToShowLayer` 把地圖拉近。
  **③點到畫面外**：行程頁的候選列實測在 y=1491 而視窗只有 900，`.planbody` 是自己的捲動容器
  ——點之前要先捲進畫面，並**斷言「它真的在畫面上」**再送滑鼠事件。
  ①是靠 **A/B 一次分清楚的**（改動前也打不開）。以下為既往：
  headless Chrome 起始明度不固定（**四次**，最近一次是 2026-09-16 單位 さ：
  「切深色之後顏色有沒有變」判 FAIL，而量到的「淺色」底色是 `rgb(20,19,17)`
  ——**它本來就已經是深色的**，於是切深色當然沒有變化，**看起來完全像產品壞了**。
  ⚠️ **修法不只是「強制 data-theme」，還要先斷言「淺色真的是淺的」**（量亮度 > 200）：
  只強制不驗的話，哪天強制那一行寫錯了會退回一模一樣的假失敗。
  補上之後實測起始值確實是 `dark`）、**同一個 Chrome profile 跨測試殘留 `localStorage`（兩次）**、
  `events.json` 有同名活動而測試比對到錯的那一筆、Cloudflare 對 `/index.html` 回 308 而腳本沒跟隨重導向、
  猜錯選擇器導致筆數查到 0、**Chrome 快取住舊版 ES Module 導致修好的錯誤照樣重現**（2026-08-12）、
  遮罩實際上有蓋到但截圖上看起來沒有（2026-08-21，量像素才確認）、
  **捲動測試往下捲了 0px 因為那個容器早就捲到底了**（2026-08-21，`scrollTop` 已是上限）。
  **時刻的正規表達式只認 `9:30` 而漏掉「午前9時30分」，於是行政機關的頁面整批被判成
  「這頁沒有營業時間」**（2026-08-21），
  **離線測試把 `addr` 傳成空字串，`osm_addr_conflict` 就剔除所有帶 `addr:city` 的候選
  ——測試自己造出一個假失敗**（2026-08-21）。
  **2026-08-23 的截圖驗證一次踩到兩個**（第十六、十七次，兩次都長得像「排序沒生效」）：
  **①沒捲到真正的底部**——圖是 lazy 載入的，`scrollHeight` 隨著載圖一直在長，
  `scrollTo(0, scrollHeight)` 打的是當下那個值，要捲到**高度連續數次不變**才算到底；
  **②拍到的是上一支測試留下的狀態**——那支測完把排序切到「快結束」就結束了，
  分頁停在那裡，截圖裡的底部理所當然不符預期。
  **拍照前先重載，並且把「目前是哪個狀態」也一起印出來。**
  兩次都是靠「回去查那幾張卡片的資料欄位」才分辨出是測試錯了。
  **2026-08-26（單位 J）又踩兩次，兩次都與「全域狀態」和「重畫」有關**：
  **①「Day2 抽不到 Day1 的站」是假的通過**——切天之後又點了一次「想跑遠一點」，
  而 `store.planFar` 是**全域、切天不會重設**，那一點等於把它關掉，於是 Day2 量到的是
  **另一個池子**，「0 次」被池子換掉解釋掉了。改成兩邊條件對齊（池子 79 個站）之後
  才量得到真的差別：同兩站在 Day1 抽到 2 次與 11 次、在 Day2 是 0 次與 0 次。
  ⚠️ 更早還有一版更糟的：拿「Day1 空的時候洗 12 次」當母體，但核心區池子有 148 個站、
  同一筆本來就很少重複出現（實測母體命中 0/2）——**那個測試通過與否都證明不了任何事**。
  **②「餐廳加不進去」是連按了同一家店兩次**：加入之後那一段會重畫、第一家變成「✓」，
  再點同一個選擇器等於把它移除，結果是「0 家」。
  **分辨方法是先在單日路徑重跑一次**（那是既有行為），一次就看出不是產品問題。
  二十二次全部一度被當成程式缺陷追查。實務做法：
  - 測試開頭一律重設 `jpev_plan`／`jpev_lang`／`jpev_theme`，別依賴 profile 的乾淨。
  - **腳本開頭送 `Network.setCacheDisabled`**，並在懷疑程式之前先 `curl` 一次確認 server 送的是新版。
  - 選擇器要**從實際 DOM 取得**再寫進腳本，不要憑印象猜（`#restList` vs `#restaurantCards`）。
  - 比對 `events.json` 的活動時注意**同名活動真的存在**（地雷 #11），對不出唯一解就跳過，不要猜。
  - **量的是「機制有沒有動」，不要只看畫面結果。** 2026-08-21 做 A/B 時，「改回舊寫法」
    那一輪竟然也通過——實際上是 Chrome 拿了快取住的舊版 ES Module。改成在 `onMove` 裡
    加一個計數器、直接問「它到底有沒有被呼叫」（答案是 0 次），一次就分清楚了。
  - **A/B 對照時要先確認「兩邊真的只差你要測的那一件事」。** 全域狀態（`store.planFar`
    那種）不會跟著你切換情境重設，順手多點一下就會把整個對照弄髒，而**兩邊各自看起來都正常**。
  - **統計型的斷言要先量母體。** 「它沒出現」在一個幾百筆的隨機池子裡是常態，
    證明不了任何事；要嘛把池子縮到那一筆會重複出現（本專案是「想跑遠一點」＝79 個站），
    要嘛換一個確定性的觀察點。
  - **最有效的分辨方法是 A/B 對照**：把同一份腳本跑在「改動前」與「改動後」兩個版本上，
    若兩邊都失敗，那就不是這次改動造成的。2026-08-05 拆檔時靠這招立刻釐清了三個誤報。

- **不要在這個專案用 `git add -A`。** repo 根目錄就是網站根目錄（地雷 #14），誤加的暫存檔會直接
  被發布到自家網域下。要逐檔指定。（2026-08-05 誤加過 `_symuse.json`。）

---

## 6c. 委派 Codex —— **已停用（2026-08-12）**

> 使用者 2026-08-12 明確表示：**不會再在這個專案呼叫 Codex**。
> 原本這裡有一整套 `codex exec` 的操作規範（背景 Bash、輸出轉向、`resume` 退修、費用估算），
> 已隨本次改動移除。**不要再嘗試呼叫 `codex`／`mcp__codex__codex`**，
> 也不要因為看到舊 commit 或 `progress.md` 第四十～四十二筆提到它就以為還能用。
> 需要查當時怎麼做的，去看 git 歷史（commit `2ec1d7b`）。

保留兩條可跨情境套用的教訓，與 Codex 本身無關：

- **範圍寫太窄會讓下屬「正確地做錯事」。** 曾限定「只用 PR TIMES 的官方新聞稿」，
  於是找不到 2024–2026 的榜單——指令被忠實執行，錯的是指令。
- **驗收標準要看用途，不是越嚴越好。** 曾要求「精確定位」，於是「只到町丁目中心」
  的結果被全數拒絕，座標覆蓋率只有 15%；改成 `precise`／`approx` 兩段制後變 99%。
  町丁目中心在東京只有 100～300m 誤差，而本站活動的「概略位置」誤差是 5～30km。

---

## 7. 環境差異備註（vs. Artifact）
- 本機開發可用 localStorage／IndexedDB 除錯；**線上的持久化機制一律以 §8 記載為準**——別把本機除錯用的儲存當成正式方案。
- 本機可實跑、可 git、可 build/test —— review 標準相應提高。

---

## 8. 專案技術背景【由自舉訪談填寫，或手動填入】

> 首次「初始化這個專案」時由 Claude Code 訪談補齊。金鑰類不寫此處。
> 本檔 §0～§7 自 kanto-events（2026-09-23）原樣複製。**2026-09-23 初始化時已檢視**：
> §5 待辦來源改指 `development-plan.md`、§6b 專案路徑改成本專案；其餘條目是跨專案通用的教訓，保留原文
> （裡面的單位代號、日期、筆數都是**日本版**的）。
>
> ⚠️ **前端是日本版同一份程式，日本版 §8 上百條前端地雷這裡照樣會踩**
> （z-index 被蓋住、`[hidden]` 藏不掉、QR 深色掃不到、iOS 主畫面、地圖卡頓…）。
> **改前端之前先去 `_ref-kanto/CLAUDE-kanto.md` 的 §8 搜尋相關段落。**

### 基本資料（2026-09-23 初始化訪談）

| 項目 | 值 |
|---|---|
| 站名 | 中文 **寄道日和・台灣**、日文 **寄道日和・台湾**，副標 `YORIMICHI BIYORI`（與日本版共用；320px 手機上餘 50px，加 `· TAIWAN` 會被截掉 12px）。日本版是「寄道日和」 |
| 網址 | `events.tw.rensakobo.com`（⚠️ 憑證未驗證，見 `development-plan.md` 單位 A） |
| **設計期間的 repo** | **公開** `JP-study-list/taiwan-events`（2026-09-23 起）。⚠️ **推上去的東西＝公開**，拆分後也收不回來 |
| 拆分後（設計結束） | 私有 repo（程式＋資料）＋公開 runner repo（只放 workflow，範本在 `_runner-template/`）；名稱拆分時定 |
| 部署平台 | Cloudflare Pages，連**拆分後的私有 repo**。⚠️ **Cloudflare 相關一律排在最後**（使用者 2026-09-23） |
| 本機舊歷史 | 分支 `local-history`（含日本版私有內容），**只留本機、絕不 push** |
| `_ref-kanto/` | **只在本機**（`.gitignore`），換電腦要自己從 kanto-events 複製 |
| 時區 | `Asia/Taipei`（UTC+8） |
| localStorage 前綴 | `twev_`（2026-09-23 已改，單位 D-3）。§6b 教訓裡的 `jpev_*` 在本專案對應 `twev_*` |
| 首發範圍 | **只有活動**；景點、餐廳分頁先藏起來，飯店更後面 |
| cron 時間 | 未定。⚠️ 要與日本版錯開（Gemini 免費額度若共用；三支 workflow 推同一 repo 會撞） |
| 待辦來源 | `development-plan.md` |

### 已知地雷（台灣版，2026-09-24 起）

1. ⚠️⚠️ **Python 3.13+ 連台灣政府網站會憑證驗證失敗**（`Missing Subject Key Identifier`）。
   **curl 連得上、Python 連不上**，失敗的樣子是「抓取失敗」「圖片查詢失敗」，看不出是憑證。
   `fetch_events.py` 檔頭的 `GOV_SSL` 全域只拿掉 `VERIFY_X509_STRICT`（鏈與網域照驗）。
   **不可改成不驗證**。Actions 目前是 3.12 所以不會發作，升版那天會。
2. **文化部的 `imageUrl` 大半是兩段黏在一起**（`https://cloud.culture.twhttps://cloud.culture.tw/...`），
   `_moc_img` 取最後一個 `http`。而且**圖片本來就不到 2%**（待辦 N）。
3. ⚠️ **台灣地址要用 Nominatim「結構化」查詢，而且一定帶「區」**（2026-09-24 實驗，E-5）。
   整串地址丟進去（Nominatim 的 `q=`、Photon）幾乎全部查無，Photon 還會錯上百公里；
   拆成 `street`／`city`（區）／`state`（縣市）就查得到（60 筆命中 46、中位誤差 40m）。
   **只給縣市不給區**：「臺南市中西區中正路1號」配到玉井區的中正路1號，差 30km 而且配到門牌＝標成精確。
   ⚠️ 但**不可以拿「查回來的區名要一致」當守門**：衛武營地址寫鳳山區、OSM 登記苓雅區（橫跨交界），結果是對的。
   地址欄寫「臺」、地標名常寫「台」，比對前先 `norm_tai`。查名稱也會回錯東西
   （「臺北市政府」→政大附中、「桃園市政府」→觀音的鋼鐵廠），**查到的座標要核對**。
4. **官方座標也會錯，而且分兩種**：①離譜的（1,016 筆裡 3 筆，最遠 296km）→ `moc_convert` 的距離守門；
   ②**差幾公里的**（抽樣 40 筆展覽裡 4 筆，2.6～7km，反查都落在別條路上；表演 20 筆 0 筆）→
   main() 拿地址交叉比對，門牌吻合且差超過 1km 就改用地址（全量實跑 5／56 被改）。
   兩種都是**實心的精確圖釘**，看起來完全正常。
5. **巡迴演出的幾個場地共用同一個網址**，用網址去重只會留下第一個場地——所以開放資料帶
   `src_key`（UID＋場地），`url_dedup_key` 優先用它。同一天、不同場地的同一檔節目 `id` 會撞號，
   只在撞號時把場地算進 `id`。
6. 國土地理院（`reverify_coords`）只收日本，**暫時 `REVERIFY_PER_RUN = 0`**；E-5 換掉前不要打開，
   否則會把台灣的正確座標誤判降級。
7. 小事：zsh 的 `$UID` 是唯讀的系統變數，腳本裡別拿它當變數名（會報 `bad math expression`）。
8. ⚠️ **文化部 API 會回「部分資料」**（2026-09-24 實測）：午夜前音樂 496 筆，凌晨 2:40 只回 23 筆（戲劇 265→23），
   HTTP 正常、JSON 正常，**看不出是不完整的**。原因未證實（推測深夜重建資料）。累積式合併會保住既有活動，
   所以不會掉資料，但**排程時間要避開深夜並實測**（單位 F）。本機驗證時看到筆數驟減，先懷疑來源、別懷疑程式。
9. **名稱比對的兄弟設施**：「…旗津分館」對「…總館」字元重疊 0.78、「國立臺灣美術館」對「國立臺灣博物館」0.71，
   原本都判 strong。`_sibling_facility` 擋掉「扣掉共同開頭後兩邊都剩設施字結尾的尾巴」；
   一邊的尾巴是另一邊尾巴的結尾（「亞洲大學現代美術館」對「…附屬現代美術館」）不算。
10. **背景跑的實驗要確認只有一支在跑**：2026-09-24 用 `nohup … &` 重起實驗，舊的那支沒停掉，
    兩支同時打 Nominatim（限速 1 req/s）、寫同一個 log，進度交錯看起來像「一筆卡兩分鐘」。
    長時間的腳本改用 Bash 的 `run_in_background`（工具會追蹤），起之前先 `ps` 確認沒有殘留。
11. ⚠️⚠️ **GitHub Models 已於 2026-07-30 全面停止服務**（2026-09-24 實測＋官方 changelog）。
    `models.github.ai` 任何路徑都回 **HTTP 200、text/plain「OK」**——不是錯誤碼，所以管線不會淘汰它，
    只會一直「回應解析失敗」重試。台灣版已從供應商鏈拿掉（`build_provider_chain`），**現在 AI 只剩 Gemini 一家**
    （Groq 從未設定），見待辦 O。⚠️ **日本版的管線應有同樣的問題**（本 repo 不改它，已告知使用者）。
    ⚠️ 本機驗證 AI 那段**只能用 Gemini 金鑰**（放 `.env`，已在 `.gitignore`）；`gh auth token` 那條路已經不通。
    ✅ 也可以在 Actions 上跑：repo secret `GEMINI_API_KEY`（2026-09-24 設好）＋ 手動觸發 `.github/workflows/test-run.yml`，
    結果在 artifact（`gh run download <id> -n test-run-output`）。
12. **Actions 的 secret 要加在「Actions」那一區**：使用者第一次加到了 **Codespaces** secrets（同一頁的另一個選項），
    Actions 讀不到、`gh secret list` 也看不到，監看等了 30 分鐘才發現。手機版直接開
    `github.com/<repo>/settings/secrets/actions` 最不會走錯。
13. **測試替程式把錯藏起來**（2026-09-24）：觀光署轉換回傳的清單混著 `drop()` 的 None，離線測試**自己先 `if e` 濾掉了**，
    於是 Actions 上 merge 撞到 None 整支中止。**驗「回傳值」的測試不可以先加工回傳值。**
14. 地雷 8 的補充：同一天下午 3 點抓，文化部音樂類是 500 筆（凌晨 2:40 只有 23）——支持「深夜回部分資料」的推測。
15. **Photon 對場館名的寫法很挑**：「台北華山1914文化創意產業園區」查無（OSM 登記「華山文創園區」，「華山1914」也查得到）、
    「衛武營…中心音樂廳」查無而拿掉「音樂廳」就有。`venue_variants_tw` 依序試幾種寫法；
    ⚠️ 剝開頭城市名只當**額外**寫法、剝完至少 4 字——「臺中國家歌劇院」的臺中是館名、「臺南美術館」剝成「美術館」會亂配。
16. ⚠️⚠️ **拿「為了排版截短的字串」當測試輸入**（2026-09-24，單位 N）：列印網址時切到 40 字，
    再把印出來的 `opentix.life/program/2074437`（真正的節目編號是 19 位數）拿去測 → 404，
    於是**誤判成「OPENTIX 只給瀏覽器看、擋爬蟲」**，還因此請使用者決定要不要用瀏覽器 UA（使用者同意了）。
    同一批**完整**網址重測，爬蟲 UA 與瀏覽器 UA 都是 200＋有圖——根本沒有擋。Jina 拿到「通用頁」也是同一個原因。
    **做 A/B 時兩邊一定要用同一份完整輸入**；看到「每一筆都失敗」先懷疑輸入本身。
17. **圖片縮圖代理（images.weserv.nl）第一次很慢**：它在歐洲，要先從台灣抓原圖再縮，實測文化部一張 904KB 的圖第一次 11.5 秒、
    第二次 0.2 秒（有快取）；觀光署一張 4.4MB 的圖代理直接 404，前端退回原圖（看得到但很重）。
    截圖裡大片灰色方塊多半是「有圖但還在載入」，**不是沒有圖**（沒圖的會畫分類色塊）。
    後續實測（2026-09-24，單位 P）：①**41 個圖片網站裡 17 個代理根本抓不到**（多是國家風景區 `*-nsa.gov.tw`、觀光署、台南、宜蘭），
    白等 6～16 秒回 404 才退回原圖 → 管線 `mark_proxy_failures` 每輪實測、標 `img_direct`，前端直接載原圖（第 3 秒載好 2→16 張）。
    ②「原圖先」雖然快（第 8 秒 24 對 16 張），但一個畫面流量 11MB→41MB，使用者選省流量，其餘維持代理先。
    ③**在海外預熱代理快取無效**：預熱過的圖從台灣載入照樣 edge MISS、代理 MISS（快取分地區）。
    ⚠️ A/B 做圖片載入時**第二輪會吃到第一輪預熱的快取**——要用沒用過的縮圖寬度（`IMG_W`）逼出冷快取，兩邊才公平；
    另外只算**畫面內**的圖（`loading="lazy"`，畫面外的本來就還沒載）。根本解法：上線時改 Cloudflare 在亞洲縮圖（單位 F）。
18. ⚠️⚠️ **新增任何政府開放資料來源，一定要在 `js/config.js` 的 `OGDL_STATEMENTS` 補顯名聲明**（2026-09-25，單位 K）。
    政府資料開放授權條款－第1版：**「未盡顯名標示義務者，視為自始未取得開放資料之授權」**——不標示＝沒有授權。
    格式照條款附件（提供機關、年份、資料名稱、條款全文與網址），日文介面照樣放中文原文。
    文化部（資料集 6478）與觀光署（資料集 7778）的授權方式已在 data.gov.tw 查證。

### Secrets 指標（**值不寫進本檔**）

- runner repo 要用的 PAT：**另申請一把只授權 `taiwan-events` 的**，存 runner repo 的 GitHub Secrets
  （名稱未定；日本版的 `KANTO_PAT` 只授權 kanto-events，不能共用）。
- LLM／抓取金鑰（`GEMINI_API_KEY`、`JINA_API_KEY` 等）同樣只放 GitHub Secrets。
  與日本版共用同一把的話，免費額度也共用。

### 已定案（2026-09-23，初始化前先記下，細節與理由見 `progress.md` 第一筆）

- **本專案是台灣版「寄道日和」**，自日本版 `kanto-events` 分出、完全獨立；**不要回頭改日本版的任何檔案**。
- **網址 `events.tw.rensakobo.com`**（兩層子網域，⚠️ **HTTPS 憑證未驗證，第一件事就是實測**；網址是最不可逆的決定）。
- **活動・景點・飯店・法規全部是台灣的**；介面**中文與日文**都要。
- ⚠️ **活動 `id` 用中文標題算**（日本版用日文，因為那是原文；這裡方向相反）。
- ✅ 兩支工具指回日本的預設路徑已於 2026-09-23 拿掉（單位 C）：`codex_kit.py` 要 `--kit`、`build_photos.py`／`pick_photos.py assign` 要 `--src`。
- 日本版的完整開發歷史在 `_ref-kanto/progress-kanto.md`，是參考資料不是本專案的進度。
