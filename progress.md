# progress.md — 開發歷史（台灣版寄道日和）

> 反向時間序（最新在上）。每次改檔即更新。
> 欄位：日期 / 類型（新增·修正·重構）/ 影響檔案 / 摘要 / 原因 / 待辦
>
> ⚠️ **日本版的完整開發歷史在 `_ref-kanto/progress-kanto.md`**（2026-09-23 原樣複製，1.3 MB）。
> 那是**參考資料不是本專案的紀錄**：開場 SOP 讀的「最新 3～5 筆」一律指本檔，
> 不要去讀那一份的開頭當成本專案的進度。要查某個做法當初為什麼那樣決定，再去那邊搜尋。

---

## 2026-09-26（第三筆）

- 類型：新增（**單位 H-4 景點照片挑圖頁、H-5 顯名聲明**；首發範圍定案）
- 影響檔案：`places_photo_tw.py`（新）、`pick_photos.py`、`places_src/_h4_photo/`（新：`index.html`、`e2e.mjs`、`README.md`）、
  `places_src/_photocache.json`／`_photowiki.json`（新）、`js/config.js`、`.gitignore`、`development-plan.md`、`project-index.md`、`CLAUDE.md` 地雷 23、本檔
- 摘要：
  - **首發範圍**（使用者）：活動＋景點；餐廳、飯店之後；AI 備援之後再補。
  - **H-5 顯名聲明**：資料集 7777（觀光署「景點 - 觀光資訊資料庫」）在 data.gov.tw 查證是政府資料開放授權條款第 1 版。
    聲明**跟著景點開關**（`FEATURES.places`）才出現。瀏覽器驗：開時中日各多一條、關時沒有，JS 錯誤 0。
  - **H-4**：`pick_photos.py fetch --wiki` 改成**中文名先查中文維基**、查不到才日文維基（條目命中 223／363）；
    修一個會讓整支中止的錯（照片沒有中繼資料時 API 回 `[]` 不是 `{}`）。座標找 2,022 張、名字找 1,772 張。
    使用者今天在手機上挑，所以日本版的本機挑圖頁不能用 → `places_photo_tw.py build` 下載縮圖（320px WebP，2,921 張）包成
    每區一個資料檔，挑圖頁放 claude.ai（claude.ai/artifact/5uAo9pGZL6RK5Eio4Lg5kx，資料庫 `pick/aNN` 存**檔名**不存索引）。
    「先幫你選」＝維基條目排第一的照片且條目座標不遠（183 筆，可換）；要挑 132；沒有候選 48。
  - 維基條目座標離我們很遠的幾筆逐一反查：太平雲梯（30km）、老塘湖藝術村（21km）是**維基座標錯**，我們的座標落在正確鄉鎮；
    鹿港龍山寺（111km）維基那條整筆不收、附近照片照樣有；中央公園配到紐約那座（被距離擋掉，正確）。
- 驗證：端對端 23 項（手機觸控模擬、假資料庫通知亂序、連續點三張卡、取消、放大後選、清本機紀錄重開從資料庫還原、
  建議可取消、換地區、434 顆按鈕全點得到、無橫向捲動）全過；**突變測試**拿掉「舊通知不蓋本機」防線 → 4 項 FAIL。
  過程中兩次「全部失敗」都是測試環境（缺編碼外殼、按鈕貼底邊 0.05px），但也因此抓到頁面真的缺 `[hidden]` 規則（地雷 23）。
  發布後：22 個檔案都在線上、資料庫 `pick` 空的（尚未挑）。
- 待辦/已知問題：等使用者挑完 → export → apply → build_photos。48 筆沒有照片會用分類圖示；觀光署照片授權未查。

---

## 2026-09-26（第二筆）

- 類型：新增（**單位 H-2 日文名套用、H-3 景點 13 類前端**）
- 影響檔案：`places_src/taiwan.json`、`places.json`、`js/config.js`、`js/icons.js`、`development-plan.md`、`project-index.md`、`CLAUDE.md` §8 地雷 22、本檔
- 摘要：
  - **H-2 日文名**：審閱頁資料庫 `ja` 集合是空的（使用者說沒問題、沒改任何一筆）→ 363 筆照初稿寫入、重跑 `build_places.py`（363／擋下 0）。
    掃出 6 筆仍是中文字形（烏來、藏金閣、德元埤、深緣、棧、檜——`jaconv.py` 的對照表沒收這幾個字；檜本來就是日文字），
    告知使用者、未改。
  - **H-3**：前端只有兩處寫死日本版 8 類——`PLACE_GENRES`（圖例順序）與 `PLACE_ICON`（圖示）；中日文名直接讀資料的 `genre`／`genre_ja`。
    沿用 7 個（博物館、美術館、公園、動物園水族館、地標展望、自然景觀、溫泉，鍵名改成新類名），新 6 個由使用者在對照頁
    （claude.ai/artifact/GKBTB7fsyH1jqpN51Map7t，圖釘實際大小＋放大＋圖例大小）挑：寺廟＝亭子（同祭典）、古蹟＝拱門、
    樂園＝雲霄飛車、老街夜市＝店面（同市集）、觀光工廠＝有煙囪的工廠、農場＝鏟子與乾草叉（使用者從拖拉機改選）。
- 驗證（瀏覽器，景點開關在測試 server 上拿掉、repo 不動）：中日各 6 項——圖例 13 行、每行有圖示、順序與文字（日文讀 `genre_ja`）、
  13 個圖示互不相同、清單 363 張卡片都有圖示且 13 種都出現；JS 錯誤 0。**A/B**：同一份腳本跑在 git HEAD 的兩支檔上，
  圖例 9 類沒圖示、順序錯、卡片 297 張沒圖示——測試有鑑別力。
  ⚠️ 第一輪兩版都是 0 行：本機沒有 `events.json`，整頁不 render（地雷 22）；第二輪卡片 0 張：景點預設是地圖模式。
- 待辦/已知問題：H-4 照片、H-5 打開景點開關。`build_places.py` 的 OSM 補位表仍用舊類名（`公園庭園`／`自然景勝`），那段不再被呼叫，跟 M 一起清。

---

## 2026-09-26

- 類型：新增（**單位 H：景點**——名單定案＋H-1 確認網頁）
- 影響檔案：`places_match_tw.py`（新）、`places_apply_tw.py`（新）、`places_src/_vote-2026-09-25.json`、`_picked-2026-09-25.json`、
  `_h1_candidates.json`、`_h1_manual.json`（新）、`.gitignore`、`development-plan.md`、`project-index.md`、`CLAUDE.md` §8 地雷 19、本檔
- 摘要：
  - **名單（2026-09-25）**：使用者不要整份觀光署資料（6,192 筆），改收「很多台灣與日本網站、KOL 都推薦」的。
    做法是每縣市找推薦文章計票（台、日文章各 1 票；遊客破百萬＋1；維基 10 語以上＋1），3 分以上收；
    文章清單靠抓小標題（h2～h4）＋自動合併寫法。使用者在逐筆按鈕的網頁上加了 32 筆、沒劃掉任何一筆 → **363 筆**。
  - **H-1**：`places_match_tw.py` 對每筆找候選地點——觀光署景點資料（同縣市、名稱相似度）→ 不夠像就 Photon →
    都沒有就用 Claude 上網查到的地址（`_h1_manual.json`，27 筆）查座標；同時猜分類、從中文維基取日文條目名、產生 `pl-` 拼音編號。
    確認網頁（claude.ai/artifact/NCx2UQMLk7vSkgV1dz63Fo，資料存在頁面資料庫 `confirm/a00`～`a19`）讓使用者逐筆選點；
    可自己貼 Google 地圖座標或網址。`places_apply_tw.py` 只收「已確認」的寫進 `places_src/taiwan.json`。
  - 分類多開一類「古蹟老屋」（第 13 類）：赤崁樓、安平古堡、紅毛城、華山、駁二這種，原本提的 12 類沒有一類貼得上。
- 原因：日本版的景點是人工整理的小清單；台灣版要全台、要有「推薦度」依據，而且座標要逐筆有人確認過（人工確認做成網頁的慣例）。
- 驗證：候選 363 筆全部有至少一個（觀光署很像的 242、地圖補 174 次查詢、人工地址 27）；座標解析單元測試 6 項（含東京座標被擋）；
  套用工具用假資料測「已確認／自己貼座標／都不對／未確認」四種情況，只寫入前兩種；確認頁以手機模擬 400px 量不到任何超出畫面的元素。
- 追加：使用者要求只看需要看的。名稱與觀光署資料或地圖幾乎一樣（分數 ≥90）的 **287 筆直接算確認**（候選資料標 `auto`，
  網頁與 `places_apply_tw.py` 都照這個標記；沒有寫進網頁資料庫——那一步被權限擋下，改成不動共享資料的做法），
  確認頁預設只剩 **76 筆**（名稱只有部分像 49、網路地址 27）。實測頁面數字 287／76、空的匯出套用後正好寫出 287 筆。
  ⚠️ 名稱比對確實會錯：「金山老街・溫泉」建議成烏來溫泉、「紅毛城」建議成小白宮——都在那 76 筆裡。
- 修正：使用者回報確認頁「有些不能點擊」。根因是固定在頂端的工具列在手機上很高，把捲到底下的選項蓋住（地雷 20）。
  改成不固定；手機模擬逐一檢查可點元素，修改前 20 多個點不到、修改後 1,337 個全部點得到。
- 修正二（使用者：「你有設計好嗎？再次確認到底可不可以用」）：讀資料庫發現使用者選過的 3 筆都存成「待確認」。
  兩個根因：①資料庫通知亂序時，舊通知把本機剛按的確認蓋回去，下一個操作再存回資料庫（地雷 21）；
  ②貼座標後直接按確認，離開輸入框觸發整頁重畫，按鈕在放開前被換掉，點擊消失。
  修正後用假資料庫（通知亂序）＋真實滑鼠事件做端對端測試：修改前 3 項 FAIL、修改後 9 項全過；手機觸控可點檢查 1,337／1,337。
  網頁原始檔與測試存進 `places_src/_h1_confirm/`（原本只在暫存區）。
- ✅ **H-1 完成**：使用者確認完 76 筆（照建議 47、換候選 5、自己貼座標 24，沒有「都不對」、沒改分類與名稱）。
  自己貼的座標與原候選距離全在 7 km 內（最遠的是原本只到鄉鎮中心的那幾筆，符合預期）。
  `places_apply_tw.py` 寫出 `places_src/taiwan.json` **363 筆**（座標來源：觀光署 222、地圖 110、自己貼 24、網路地址 7）。
- **H-2（管線台灣化）**：`build_places.py` 不再查國土地理院、不再用日本地址判地區——座標與地區直接用 H-1 確認值
  （沒有座標就擋下、叫人回確認網頁，**不讓程式猜**）；分類換成台灣 13 類（含日文名稱）；必填只剩 id 與 title
  （地圖搜尋來的景點多半沒官網、只有區級地址）；日文名空白暫時沿用中文名並在統計多印一行「日文名待補」。
  `BUILD_VERSION` 1→2。結果：輸出 363／擋下 0、查詢 0 次；test_osm_places／test_place_hours／test_areas／test_tw_geo 全過。
  日本版的查座標函式（`geocode`、`osm_rescue`）現在不會被呼叫，留給 M 一起清。
- **日文名**：263 筆由 Claude 翻（專有名詞保留漢字換日文字形、常見說法換日文、慣用名手寫；`梅花鹿` 刻意不譯成ニホンジカ），
  審閱頁 claude.ai/artifact/6ngJk2BYAnmx161awD1vPD（改過的存在頁面資料庫 `ja/aNN`），`places_apply_ja.py` 寫回清單。
  審閱頁照地雷 20、21 設計（頂端不固定、改一格只存一格不重畫、自己的通知不理），端對端測試 10 項全過。
- 待辦/已知問題：~~等使用者確認完 → 跑 apply~~ → ~~H-2~~ → 日文名 263 筆待補（AI 翻＋給使用者看）（`build_places.py` 台灣化）。9 筆人工地址只查到鄉鎮中心，要靠使用者在確認頁修點。
  日文名只有 100 筆從維基拿到，其餘之後 AI 翻、再給使用者看。

---

## 2026-09-25（第二筆）

- 類型：新增（**單位 K：法規查證與文案**；⚠️ 非律師審閱）
- 影響檔案：`js/config.js`、`js/siteinfo.js`、`development-plan.md`、`CLAUDE.md` §8 地雷 18、本檔
- 查證（WebSearch＋data.gov.tw 實查）：
  - **政府資料開放授權條款－第1版**：利用時必須依附件格式顯名，「未盡顯名標示義務者，視為自始未取得開放資料之授權」。
    文化部「藝文活動-所有類別」（6478）與觀光署「活動 - 觀光資訊資料庫」（7778）都是這份授權。**原本站上完全沒有標示。**
  - 公平會〈對於薦證廣告之規範說明〉：有利益關係要揭露；現有「廣告」標示足夠。目前站上沒有任何聯盟連結
    （票券按鈕只在景點頁與行程裡的景點，跟著 D-4 藏起來了），但隱私權頁照樣寫「是聯盟連結」→ 不符事實。
  - 著作權：單純超連結沒問題；在頁面上顯示他站圖片是模糊地帶。使用者選「繼續用售票網站宣傳圖＋出處與移除申請」。
  - 日本ステマ規制：規範對象是廣告主（自己供應商品者），現有日文「広告」標示已足。
- 摘要：網站資訊新增「資料來源與授權」（兩份顯名聲明逐字照格式，中日頁面都用中文原文，另加 OSM 出處）與「活動照片」
  （來源、不保存、點卡片連回原網頁、權利人可來信要求移除）；隱私權外部服務清單加「活動主辦單位與售票網站」、
  景點關時不列 Wikimedia Commons；聯盟連結說明改成 `AFF_DISCLOSURE && FEATURES.places`，「沒有」版本改寫成兩種情況都成立。
- 驗證：瀏覽器首發設定 16 項（中日：段落順序、兩份顯名聲明逐字含全部格式要件、清單、說明版本、移除申請）＋
  對照組（打開景點）7 項（Commons 會列、改用聯盟連結版本）全過，JS 錯誤 0。
- 待辦/已知問題：景點上線、票券連結出現時要再檢查一次聯盟揭露；新增開放資料來源要補顯名（地雷 18）。

---

## 2026-09-25（第一筆）

- 類型：修正（**單位 E-8：展覽場地只寫鄉鎮或館內空間**）
- 影響檔案：`fetch_events.py`、`test_tw_geo.py`、`development-plan.md`、本檔
- 摘要：
  - 實際量：展覽 224 筆裡 **34 筆只寫到鄉鎮**（格式「鹿港鎮（彰化縣）=」），只寫館內空間的其實只有「第N展覽廳」這種
    （「中正紀念堂1展廳」「富邦美術館1樓」本身有館名，不處理）。
  - `moc_venue`：從演出／主辦單位挑**像場館、不像機關**的名字（`_place_like`，排除公所、文化局、協會、志工、管理處…）。
    ⚠️ 第一版 17 筆裡錯 7 筆——志工團被當場地、**主辦的博物館在別處辦展**（高雄市立歷史博物館在鹽埕、展在左營）
    → 加 `_found_elsewhere`：Photon 查名稱（⚠️ **不加縣市**，加了只會在那一帶找，台北的郵政博物館就「查不到」而被放行），
    **明確在別的鄉鎮區才否決**，查不到照樣採用；本區查到更具體的名稱就用它（「郵政博物館」→「郵政博物館高雄館」——
    原本以為是誤判，查了才知道高雄真的有分館）。沒有可用館名時清成乾淨的「鹿港鎮」。
- 驗證：規則 10 例全過；真實資料逐筆審閱（12 個候選：10 採用、2 在別處維持鄉鎮，全對）；
  整合跑 `moc_events`（47 秒）：只寫鄉鎮 34→24、「（縣）=」尾巴 0、第N展覽廳剩 2（藝術教育館，資料沒有館名線索）。
  `test_tw_geo.py` 加 [5c] 9 項（查詢換成假的，不打網路）；**突變測試**：拿掉「在別處」查證、拿掉「志工」都抓得到
  （原本「志工」那例同時命中「管理處」、單獨考驗不到——補了「…博物館志工隊」一例）。
- 待辦/已知問題：E-11（國字門牌，7 筆、6 筆有官方座標，先不做）。

---

## 2026-09-24（第十筆）

- 類型：新增（**單位 D-4、D-6、D-5**，使用者指定順序）
- 影響檔案：`index.html`、`js/config.js`、`js/main.js`、`js/mapswitch.js`、`js/tour.js`、`css/style.css`、`development-plan.md`、本檔
- 摘要：
  - **D-4 藏起景點與餐廳**：開關只有一處——`index.html` 的 `<html class="no-places no-restaurants">`
    （放在 HTML 上第一次繪製就藏好，不會閃）；`config.js` 的 `FEATURES` 讀它。CSS 以 `!important` 藏入口
    （分頁、餐廳收藏、加一頓飯、圖片出處；`#foodSec` 有 JS 寫的 inline style、`.tab` 是 flex，不加會被蓋過）；
    地圖切換器只剩活動一張時整個不畫；關閉時不抓 `places.json`。
  - **D-6 介面文字**：資料來源、關於本站（中日）、「想跑遠一點」（桃園・新竹・基隆・宜蘭／台北・新北）、
    網頁描述（原在 D-2，文字不必等網址）；導覽「這個網站分幾塊」改成**依 FEATURES 算**，之後開景點／餐廳不必再改。
    隱私權與廣告標示留給 K，並在 K 補記「外部服務清單要加圖片來源網站與縮圖代理」。
  - **D-5 日本版連結**：設定頁最下面一張入口卡，`<a target=_blank>`、外部連結圖示（Tabler 原檔）。
- 驗證（瀏覽器，全部 JS 錯誤 0）：
  - D-4 **A/B**：開關生效 9 項、拿掉開關（對照組）9 項都過——證明藏起來的確實是開關的作用。
    ⚠️ 第一版「加一頓飯」那項斷言把「JS 自己藏的」也算成看得到、兩邊分不出來；改成模擬 JS 打開它再看 CSS 擋不擋。
  - D-6 中日 15 項：畫面上掃不到日本地名、導覽第二步是「三塊／3 つ」、網頁描述是台灣。
  - D-5 中日 14 項：文字、連結、開新分頁、與「網站資訊」卡同寬同高。
  - ⚠️ 文件更新腳本曾中途失敗（D-2 那列的原文與預期不同），commit 照樣跑了半套 → 補一個 commit 修齊。
- 待辦/已知問題：D-2（網址，等 A）、D-7（iPhone 實機，等部署）；日本版那一側的連結要回日本版 repo 做。

---

## 2026-09-24（第九筆）

- 類型：修正（**單位 P：圖片載入慢**，部分完成）
- 影響檔案：`js/util.js`、`fetch_events.py`、`test_tw_geo.py`、`development-plan.md`、`CLAUDE.md` §8 地雷 17、本檔
- 摘要：
  - 根因調查：前端經 images.weserv.nl（歐洲）縮圖。16 個圖片網站實測：直連 0.2～1 秒、代理 1.3～16 秒；
    **部分政府網站代理根本抓不到**（白等 6～16 秒回 404 才退回原圖）；高雄旅遊網則相反（直連 403、要靠代理）。
  - 試過「原圖先」：冷快取公平 A/B（逼出冷快取要換沒用過的 `IMG_W`），第 8 秒 24 對 16 張、但流量 11MB→41MB。
    試過「海外預熱」：經 Jina（海外）預熱後從台灣載入照樣 MISS，**無效**。→ 使用者選「代理先＋抓不到的直連，上線改 Cloudflare 縮圖」。
  - `mark_proxy_failures`：每輪對每個圖片網站抽 2 張用最小寬度問代理（不複製前端縮圖參數），都失敗才標記整站 `img_direct`；
    並行 8、實測 41 個網站 52 秒，**17 個抓不到**（多為國家風景區 `*-nsa.gov.tw`）。前端 `imgChain` 遇到 `img_direct` 直接載原圖。
  - `clean_img_url`：主機名稱不是公開網域的不採用（文化部資料有 `http://data-service/api/…` 這種內部主機名稱）。
- 驗證：被標記的 84 張 A/B（冷快取）：第 3 秒載好 **2 → 16** 張、第 8 秒 5 → 18 張；逐一重測 3 個被標記網站確認真的抓不到。
  ⚠️ 第一次 A/B 的第二輪吃到第一輪預熱的快取，結論一度反過來——已改用冷快取重做。`test_tw_geo.py` 加內部主機名稱一項，全部測試通過。
- 待辦/已知問題：代理第一次載入仍要 1～7 秒 → F（Cloudflare 縮圖）。

---

## 2026-09-24（第八筆）

- 類型：新增（**單位 N：活動圖片**）
- 影響檔案：`fetch_events.py`、`js/cards.js`、`css/style.css`、`development-plan.md`、`CLAUDE.md` §8（地雷 16、17）、本檔
- 摘要（使用者 2026-09-24：「很想要圖片」→ 兩個都做）：
  - **補宣傳圖** `fill_og_images`：沒圖的活動去官方頁抓 og:image。文化部活動的連結 76% 是 OPENTIX、54 筆年代售票，
    實測都有節目主視覺。誠實爬蟲 UA、每秒最多 1 頁、同一網址只抓一次、同一張圖被 ≥3 頁共用＝網站橫幅不採用、
    Facebook 等要登入的不抓、`og_tried` 14 天內不重抓、整段 25 分鐘上限（沒抓完下一輪繼續）。排在 `verify_images` 之前。
  - **沒圖時的色塊**：淡色底（淺 9%／深 14% 分類色，`--block-tint`）＋分類圖示＋分類名，兩個入口共用 `blockHTML`。
    對照圖 `_probe/n-noimg.png`，使用者選 C。仍守原本「不整片塗分類色、免得比真照片搶眼」的原則。
  - ⚠️⚠️ **差點做錯的一件事（地雷 16）**：用截短的網址測試，誤判「OPENTIX 只給瀏覽器看」，請使用者決定要不要用瀏覽器 UA
    （使用者同意了）。用同一批完整網址重測發現根本沒擋 → **全部改回誠實的爬蟲 UA**，已向使用者說明。
- 驗證：
  - 補圖實測 60 筆補到 49（OPENTIX 43／年代 4／其他 2），文化部整站橫幅被擋下。
  - 瀏覽器 10 項：淺深兩色的沒圖卡片都有圖示、分類名，底色確實帶分類色。
  - Actions 實跑（58 分）：補圖撞到 25 分鐘上限，414 頁補到 334 筆，擋下 3 種網站橫幅；
    **有圖比例 全部 39%**（觀光署 100%、網站 77%、文化部 2%→29%，還有 670 筆沒輪到，正式上線後 2～3 天補齊，預估文化部約 8 成）。
- 另外查到（待辦 P）：截圖裡的大片灰色多半是「有圖但還在載入」——縮圖代理 weserv 第一次要 10 秒、第二次 0.2 秒；
  4.4MB 的圖代理直接失敗、前端退回原圖。
- 待辦/已知問題：P（圖片第一次載入慢）；補圖每頁實際 3.6 秒（網站回應約 2.6 秒＋間隔 1 秒），要更快可以並行但總速率仍限每秒 1 頁。

---

## 2026-09-24（第七筆）

- 類型：新增＋修正（**E-4 第二段在 Actions 上實跑驗證、跨來源去重、場館名多種寫法**）
- 影響檔案：`fetch_events.py`、`test_tw_geo.py`、`.github/workflows/test-run.yml`（新）、`development-plan.md`、
  `project-index.md`、`CLAUDE.md` §8（地雷 11 補充、12～15 新增）、本檔
- 摘要：
  - 使用者不在電腦前，改在 **Actions 上實跑**：新增只能手動觸發的 `test-run.yml`（結果存 artifact、不 commit 回 repo）；
    使用者用手機申請 Gemini 金鑰、加 repo secret（第一次加到 Codespaces 那區，地雷 12）。
  - 第一次實跑 4 分鐘就中止：觀光署轉換回傳混著 None（地雷 13）→ 根因修在 `tb_events`，主流程再加一道過濾。
  - 第二次成功（28 分）：1,441 筆、日文 100%、AI 分類合理（「不收」54 筆幾乎都照規則，遊程與優惠都擋掉）。
    但發現**重複收錄**：同一活動被不同機關各登一次、展期被拆兩段 → 新增 `dedupe_events`
    （同桶＋標題正規化相同＋日期碰得到＋場地對得上；保留順序固定，id 才穩）。1,441 筆 26 組可疑，
    逐組看過合併的 20 組全對、不該合的（巡演不同場地、同期不同場館）都沒合。
  - 網站來源有幾個知名場館查不到（華山、衛武營…音樂廳）→ `venue_variants_tw` 多試幾種寫法（地雷 15），
    差點把「臺南美術館」剝成「美術館」，改成剝城市名只當額外寫法。仍是概略位置的 30 筆救回 9 筆，全部 precise、位置逐一看過合理。
  - 第三次實跑（去重版，26 分）：**1,423 筆、合併 28、八類都有**（表演 1,023／展覽 270／祭典 47／運動 40／
    市集 25／動漫 12／點燈 5／煙火 1）、精確 1,386（97%）、有圖 234、id 不重複、暫存欄位沒寫出去。
- 驗證：`test_tw_geo.py` 加 [5b] 場館寫法、[6] 去重，**突變測試兩次抓到沒有鑑別力的案例**
  （「不看日期」與「拿掉只寫鄉鎮區」都照樣全過——原案例的場地本來就對不上／座標重疊），補案例後都抓得到。
- 待辦/已知問題：觀光署標題差幾個字的重複沒合併（寧可少合）；日文用字偶爾不一致；E-8（展覽場地只寫展廳）；
  N（圖片）；O（AI 只剩 Gemini）。
- 第四次實跑（場館寫法版，29.5 分）：1,421 筆、**概略位置 30 → 20**、精確 1,394（98%）、網站來源 19 筆中 11 筆精確（上輪 6）、
  id 不重複。⚠️ 「官方座標改用地址」這輪 19 筆、上輪 12 筆——同一份程式兩輪不同，推測是文化部資料與 Nominatim 回應每輪略有差異，
  沒有深追；若日後這個數字大幅跳動再查。

---

## 2026-09-24（第六筆）

- 類型：新增（**單位 E-4 第二段＋E-10；AI 那段未實跑**）
- 影響檔案：`fetch_events.py`、`js/config.js`、`js/icons.js`、`css/style.css`、`development-plan.md`、`CLAUDE.md` §8、本檔
- 摘要：
  - **觀光署活動資料**（使用者 2026-09-24 定案：開放資料為主、運動賽事收、遊程不收、動漫一起找）：
    找到現在的下載網址 `media.taiwan.net.tw/XMLReleaseAll_public/v2.0/Zh_tw/Event-json.zip`（data.gov.tw 7778，
    舊網址已 404）。未結束 268 筆，**全部有座標、圖片、縣市與區**。`tb_events`：AI 分類（八類或「不收」），
    分類結果存在 `events.json` 的 `tb_class`、每筆只分一次；同一座標被 ≥3 筆共用＝代表點不採信（實際 41 筆）；
    StreetAddress 有「號」才當地址、否則當場地名；沒網址（206 筆）退回 Google 搜尋。
  - **動漫網站 5 個**（用管線自己的 `fetch_page_text` 實測）：萌朧動漫情報網、輕旅行快閃店＆展覽、CWT、開拓 FF、
    台北國際動漫節。shonm32、臺北旅遊網 API 被 Cloudflare 機器人驗證擋住，**不繞過**。
  - 網站抽取指令加 `addr`（網頁上有寫才填），`clean_event` 收下，讓 AI 抽到的活動也能走結構化地址查座標。
  - **第八類「運動」**：後端 TYPES＋分類規則；前端 TYPES、中日名稱（スポーツ）、圖示跑步、四個色變數。
    顏色是**搜出來的**：九色色相在 200～279° 之間空了 79°，在「彩度明度落在現有範圍、三種情境對比都達標」
    條件下，色相 272 對最近那色 ΔE76 ≥29.8（既有最擠 14.1）。先反推出日本版用的是 ΔE76（煙火↔表演正好 14.1）。
    對照圖 `_probe/e10-sport.png`，使用者選藍＋跑步。對比：淺文字 4.63／圖釘 3.03／深文字 8.06。
  - ⚠️⚠️ **GitHub Models 已於 2026-07-30 停止服務**：使用者請我改用 GitHub 帳號跑 AI，實測 `models.github.ai`
    任何路徑都回 200「OK」（沙盒內外都一樣、DNS 與憑證都是 GitHub 本尊）→ 查官方 changelog 證實已退役。
    從供應商鏈拿掉，現在 AI 只剩 Gemini（待辦 O）。**日本版應有同樣問題，已告知使用者、沒有改它。**
- 驗證：觀光署轉換用真實資料離線 8 項全過（假分類器）；瀏覽器「運動」相關全過（分類列八類、中日名稱、
  色塊 #1875C0、圖例第八列是跑步、開地圖無 JS 錯誤；另 2 項 FAIL 是測試站資料換成實資料後期待值過期）；
  `test_areas.py` 的前後端分類一致那項先抓到「後端八類、前端七類」，補完前端後通過。
- 待辦/已知問題：**AI 那段未實跑**（觀光署分類、動漫抽取、日文翻譯）——等 Gemini 金鑰。
  這一批與上一個 commit（`6018945`）**都還沒 push**，AI 實跑過再一起推。

---

## 2026-09-24（第五筆）

- 類型：新增（**單位 E-5：台灣版查座標**）
- 影響檔案：`fetch_events.py`、`test_tw_geo.py`（新，第六支常設測試）、`development-plan.md`、`project-index.md`、
  `CLAUDE.md` §8（地雷 3、4 改寫，8～10 新增）、本檔
- 摘要：
  - **先實驗再動手**：拿 60 筆有官方座標的活動當標準答案，量五種查法（scratchpad `geo/exp.py`）。
    Nominatim **結構化地址** 46/60 命中、中位 40m 最好；Photon 場館名＋縣市 38/60 次之；
    Photon 整串地址 2/60 且兩筆都錯上百公里。**推翻了上一筆「查不到門牌地址」的結論**——是查法不對。
  - 新增 `parse_tw_addr`／`geocode_addr_tw`／`geocode_district_tw`／`resolve_coords_tw`。順序：
    官方座標 → 結構化地址（帶區；配到門牌＝precise、只到路名＝uncertain）→ Photon 場館名＋縣市
    （比對只看場館名）→ Nominatim 場館名 → AI 座標 → 區中心 → 縣市中心。日本版 `resolve_coords` 不再呼叫（單位 M）。
  - **官方座標跟地址交叉比對**：門牌吻合且差 >1km 改用地址。實驗抽 40 筆展覽有 4 筆官方座標偏 2.6～7km，
    反查全落在別條路上、地址查到的門牌完全吻合（紅毛港文化園區、亞洲大學現代美術館、水里遊客中心、旗津分館）。
  - 拿掉日本的國別過濾（`countrycodes=jp`、`"JP"`）；縣市守門與名稱比對先統一「臺」；中文通用詞；
    **兄弟設施規則**（「…旗津分館」對「…總館」原本判 strong）。`GEO_VERSION` 11→12。
- 驗證：
  - 管線自己的函式重跑那 60 筆：**precise 53 筆、中位 50m、錯 >5km 卻標 precise 0 筆**；
    precise 裡 2.6～5km 的三筆逐一查證都是官方座標錯。加兄弟設施規則前後結果一致。
  - `test_tw_geo.py` 全過；**突變四種都抓到**（不帶區、不統一臺、拿掉兄弟設施、國別改回 jp）。
    ⚠️ 測試錯了兩次：①「台南市→放行」那項沒有鑑別力（守門認不出縣名本來就放行），改成「台北市不給新北桶」；
    ②國別檢查把說明文字也算進去，改成只比對會生效的寫法。
  - 全量實跑兩次：第一次官方座標「地址確認一致 51／改用地址 5」，第二次全部沿用、0 次查詢。
  - ⚠️ **那次全量實跑只有 250 筆**：文化部 API 凌晨 2:40 只回部分資料（音樂 496→23），不是程式問題
    （CLAUDE.md §8 地雷 8）。累積式合併不會掉既有活動，但排程時段要避開（已記進單位 F）。
  - ⚠️ 過程中背景實驗**同時跑了兩支**（舊的沒停掉），看起來像查詢卡住；已記成地雷 10。
- 待辦/已知問題：E-9（複驗換台灣版，目前停用）、M（日本版查座標死碼）、G（`test_geo_gates.py`）。

---

## 2026-09-24（第四筆）

- 類型：新增（**單位 E-4 第一段：文化部藝文活動開放資料**）
- 影響檔案：`fetch_events.py`、`js/util.js`、`development-plan.md`、`project-index.md`、`CLAUDE.md` §8、本檔
- 摘要：
  - 新來源 `kind: "moc"`：抓音樂(1)、戲劇(2)、舞蹈(3)、獨立音樂(5)、演唱會(17)→表演，展覽(6)→展覽；
    親子、講座、電影不收。不經 AI 抽取，縣市從地址判斷（`county_of`，先「台」→「臺」）。
  - **按場地拆**：巡迴演出一個場地一筆；`src_key`＝UID＋場地當去重鍵（它們共用同一網址）。
    同天不同場地撞號時才把場地算進 `id`。丟掉常設展、檔期超過 400 天、認不出縣市的。
  - 座標：官方座標（有距離守門）→ `precise`；沒有的**暫時**放縣市政府、標 `area`（E-5 之前不拿
    日本那套查詢去打台灣地址）。国土地理院複驗暫停（`REVERIFY_PER_RUN = 0`）。
  - `fill_japanese`：補日文，**同 id 且中文標題與場地沒變就沿用既有譯文**，其餘每 40 筆一批請 AI 翻。
  - `--no-llm`：本機沒金鑰也能跑（只跑開放資料、日文留空）；排程上不加，缺金鑰照樣失敗。
  - `GOV_SSL`：Python 3.13+ 連政府網站憑證會失敗，全域只拿掉嚴格格式檢查（見 CLAUDE.md §8 地雷 1）。
  - 前端 `mapQuery`（Google 地圖連結）改用中文場館名與中文地區名。
- 驗證：
  - 實跑兩次（scratchpad，`--no-llm`）：1,205 筆、精確座標 1,013、概略 192；第二次「新收錄 0、id 集合相同、
    first_seen 不變」。深度檢查 10 項全過（id／src_key 不重複、暫存欄位沒寫出去、巡迴拆成台中／高雄／台北、
    桃園與富岡同天兩站都保留、沒有黏在一起的圖片網址）。
  - 實跑過程中抓到並修掉四件事：**同天不同場地的 id 撞號**（1,205→1,203，少掉富岡站）、
    **圖片網址黏在一起**、**政府網站憑證**讓驗圖 10 張全失敗（修後 9／10）、**3 筆官方座標錯**（加守門）。
  - 前端放 1,205 筆實資料 11 項全過（中日、表演筆數、20 桶筆數與資料一致、日文退回中文、
    地圖查詢用中文、開地圖無 JS 錯誤）；截圖 `_probe/e4-real-list.png`／`e4-real-map.png`。
- 待辦/已知問題：新開 **N（活動圖片，要先討論）**、**E-8（展覽場地只寫展廳）**；E-4 第二段（網站補缺）；
  E-5（查座標）。⚠️ `events.json` 沒有 commit——第一份正式資料應由排程產生。

---

## 2026-09-24（第三筆）

- 類型：修正（**單位 E-7：活動類型改七類**）
- 影響檔案：`fetch_events.py`、`js/config.js`、`js/icons.js`、`css/style.css`、`test_areas.py`、
  `development-plan.md`、`project-index.md`、本檔
- 摘要：
  - `TYPES`（前後端）＝展覽、表演、祭典、煙火、市集、動漫、點燈；日文「表演」＝公演。
  - 「表演」**接手寶可夢的色位**：`--c-／--pin-` 淺深共四處改名、色值不動（日本版八色是量過分辨度才定的）。
  - 圖示（使用者看 `_probe/e7-icons.png` 對照圖選的）：表演＝音符（票券會撞既有的 Klook 票券功能）；
    **祭典由日本鳥居換成亭子**（鳥居是神社的象徵）。路徑取自 Tabler 官方套件原檔，不手寫。
  - 給 AI 的分類規則：拿掉寶可夢那條、加「表演」（要進場看的演出）；點燈／祭典／煙火／市集的例子換成台灣的
    （燈會、光雕、耶誕城；平溪天燈與鹽水蜂炮仍算祭典；遶境、豐年祭；澎湖花火節；農夫市集）。
  - `test_areas.py` 加 [7]：前後端 `TYPES` 逐字相同、中日每一類都有名稱。突變（前端漏了表演）抓得到。
  - 新開單位 M：寶可夢行事曆那段死碼（目前不會執行）。
- 驗證：後端離線 7 項 A/B（改動前 0／7、改動後 7／7）；瀏覽器 26 項（中日兩語：分類列七類與標籤、
  沒有寶可夢、表演色塊＝#6A5A83、圖例七列且表演是音符／祭典是亭子、開地圖無 JS 錯誤）。
  ⚠️ 測試錯了一次：第一輪在 `about:blank` 上寫 localStorage（會丟例外、被吞掉），淺色沒設進去——
  **環境檢查「淺色真的是淺的」當場抓到**，而且量到的是表演在深色下的色值 #9689AD，等於順便驗了深色那格。
- 待辦/已知問題：`sourceInfo` 文案仍寫「Walker+／寶可夢官方」（D-6）。

---

## 2026-09-24（第二筆）

- 類型：重構（**單位 E-3：20 個地區桶＋五大區**）
- 影響檔案：`fetch_events.py`、`js/config.js`、`js/map.js`、`js/mapall.js`、`js/plan-ui.js`、`js/mylocation.js`、
  `test_areas.py`、`development-plan.md`、`project-index.md`、`CLAUDE.md` §6b、本檔
- 摘要：
  - 後端換掉：`AREAS`（20 桶）、`AREA_SPEC`（給 AI 的各桶涵蓋說明）、`AREA_CENTER`／`PREF_CENTER`
    （縣市政府座標）、`AREA_ADMIN`、`AREA_PREF_OK`、`PREF_ALL`（22 縣市，正式寫法「臺」）、
    `GEO_BBOX`（台澎金馬）、`AI_MAX_KM_AREA`（粗估，未校準）；拿掉神奈川排除規則與日本的場地黑名單。
  - 前端：`AREAS`、`AREA_GROUPS`（五大區）、中日地區名、`PLAN_ZONES`＝五大區、`HOME_ZONE='北部'`、
    `CORE_AREAS`＝雙北、`LOC_FAR_KM` 300→350（馬祖 291km、金門 280km）。
  - ⚠️ 地圖預設視野：新增 `LOC_VIEW_ZOOM=7`（全台），**`LOC_ZOOM` 維持 10**——它同時是「移到我的位置」
    的城市尺度，直接改成 7 會讓定位拉得太遠。活動地圖、疊圖、行程地圖、選點地圖改用新常數；
    景點與餐廳地圖跟著 H／I。
  - `test_areas.py`：守「活動 ↔ 前端」兩份一致；餐廳那份仍是日本版，暫時只提示（單位 I）；
    縣市名檢查改台灣規則，新增「22 個」「用臺」「每縣市都有桶」「中心都在框內」。
- 查證（打 API，2026-09-24）：22 個縣市政府座標用 Nominatim／Photon 查名稱取得，**逐筆核對**——
  第一次有四筆查到別的東西（臺北→政大附中、桃園→觀音的鋼鐵廠、高雄→鳳山行政中心、苗栗→消防局 2.8km），
  改用 Photon 重查。⚠️ **兩個服務都查不到台灣的門牌地址**，這改變了 E-5 的方向（已記進計畫書）。
- 驗證：
  - `test_areas.py` 全過；**突變測試三個都抓到**（前端少馬祖、宜蘭排進中部中間、PREF_ALL 寫「台北市」）。
  - 瀏覽器 26 項（假資料 6 筆分散在北中南東離島）：中日兩語的五大區與 20 桶順序、筆數、
    點「台南」剩 1 筆、點「北部」剩 3 筆、活動地圖／行程頁打開沒有 JS 錯誤、
    **選點地圖的圖磚是第 7 級且落在台灣那幾格**（x 106–107、y 54–55）、行程大區只有台灣的名字。
  - ⚠️ 測試錯了一次：地區筆數在打開篩選面板前全是 0（面板打開才重算，既有設計），已補進 CLAUDE.md §6b。
- 待辦/已知問題：查座標的日本機制（市町村清單、国土地理院反查、`pref_gate` 的臺／台）→ E-5。

---

## 2026-09-24（第一筆）

- 類型：修正（**單位 E-1 時區、E-2 id、E-6 翻譯方向**）
- 影響檔案：`fetch_events.py`、`development-plan.md`、本檔
- 摘要：
  - 前一天與使用者定案單位 E 的方向（20 桶＋五大區、七類、開放資料為主、全台），細節在 `development-plan.md` §7-E。
  - E-1：`JST = ZoneInfo("Asia/Tokyo")` → `TW_TZ = ZoneInfo("Asia/Taipei")`（連同 `updated_at`）。
  - E-6：給 AI 的欄位規則反過來——`title`／`venue` 照抄**中文原文**（臺／台都不換字），
    `title_ja`／`venue_ja`／`desc_ja` 才翻成日文；範例換成台灣場館。
  - E-2：`id_base = ev["title"]`（日本版是 `title_ja or title`）。刻意不退回 `title_ja`：
    `title` 空的在前面已被丟掉。`SCHEMA_VERSION` 沿用 2（台灣版沒有舊資料可汰換），註解寫明語意已變。
- 原因：台灣版原文是中文、日文才是會漂移的譯文（日本版地雷 #8 的反方向）。
- 驗證：離線測試 8 項，**A/B 對照**：改動前 1 PASS／7 FAIL，改動後 8 PASS。
  其中「TODAY 是台北的今天」兩邊都過（兩地只差一小時，那項本身沒有鑑別力，鑑別靠時區那一項）。
  既有三支離線測試照樣全過。
- 待辦/已知問題：⚠️ `geocode()` 仍以 `venue_ja` 為主查詢，E-6 之後那是 AI 譯文，併進 E-5 處理。
  **目前還不能實跑管線**：地區桶、類型、來源都還是日本的（E-3／E-7／E-4）。

---

## 2026-09-23（第六筆）

- 類型：修正（**單位 D-1 站名、D-3 localStorage 前綴**）
- 影響檔案：`index.html`、`js/config.js`、`js/tour.js`、`css/style.css`（註解）、`js/` 共 14 支（前綴）、
  `CLAUDE.md` §6b §8、`development-plan.md`、`project-index.md`、本檔
- 摘要：
  - D-3：`jpev_` → `twev_`，10 種鍵、37 處，全是寫死的完整鍵名（沒有拼接），逐字替換。
    台灣版沒有使用者，不需要搬舊資料。
  - D-1（使用者 2026-09-23 看對照圖決定）：中文 `寄道日和・台灣`、日文 `寄道日和・台湾`；
    副標沿用 `YORIMICHI BIYORI`；分頁標題「寄道日和・台灣｜台灣展覽、祭典與活動地圖」
    （日文「…｜台湾の展覧会・祭り・イベント地図」）。改了 config 中日兩組、`<title>`、`og:title`、
    `og:site_name`、iOS 主畫面名、導覽第一步的標題與那一句說明。
  - 量測（320px）：站名 169.7px／可用 292px，不換行；副標 `YORIMICHI BIYORI` 餘 50px，
    `· TAIWAN` 缺 12px、`YORIMICHI BIYORI TAIWAN` 缺 3px（不採用）。對照圖在 `_probe/d1-compare.png`（不進版控）。
  - 新開 D-6（其餘寫著日本的介面文案，約 36 行）、D-7（iPhone 實機確認主畫面名稱會不會被截斷）；
    `description` 文案併進 D-2。
- 驗證（Chrome headless＋CDP，假資料放 scratchpad）：
  - D-3 共 10 項：寫入的鍵全是 `twev_`、沒有 `jpev_`；系統固定深色時存 `twev_theme=light`
    會變淺、存 `twev_lang` 會切語言；**對照組**只存舊的 `jpev_` 鍵則完全沒效果。
  - D-1 共 20 項：靜態 HTML 四處、中日兩語的分頁標題／頂欄／副標／導覽標題，320px 不換行不截斷，JS 錯誤 0。
  - ⚠️ **測試自己錯了三次，三次都長得像產品壞了**（已補進 CLAUDE.md §6b）：
    ① `load()` 只等 300ms，讀到上一頁；② **別的專案也在用 8899 埠**，瀏覽器被導到對方的伺服器
    （localStorage 冒出 `poke-change/v1`）；③ `python3 -m http.server` 等候佇列只有 5，
    約 1/8 的載入有模組被 `ERR_CONNECTION_RESET`、整頁 JS 不跑。改用專用埠 8931／9241、
    綁 127.0.0.1、佇列 128 的伺服器之後才全過。
- 待辦/已知問題：D-2、D-4～D-7 見 `development-plan.md`。

---

## 2026-09-23（第五筆）

- 類型：修正（**單位 C：工具去日本化防呆**）
- 影響檔案：`codex_kit.py`、`build_photos.py`、`pick_photos.py`、`build_restaurants.py`、`fetch_events.py`、
  `development-plan.md`、`project-index.md`、`CLAUDE.md` §8、本檔
- 摘要：
  - C-1／C-2：`DEFAULT_KIT`、`SRC_DEFAULT` 改成 `None`，沒指定路徑就停下來說明要用 `--kit`／`--src`。
    **沒有改成「台灣的某個路徑」**：那兩個資料夾要看單位 L（要不要接外部代理）與 H（原圖放哪），還沒定。
    ⚠️ 盤點時多找到一處：`pick_photos.py assign` 也吃 `build_photos.SRC_DEFAULT`，一起擋。
  - C-3：`kanto-events-*` 兩處與 `fetch_events.py` 送給查座標服務的 `jp-events/2.0` 四處，
    改成 `taiwan-events-*`，聯絡網址用 GitHub repo（網站網址要到單位 A 才定）。
  - `fetch_events.py` 檢查圖片時送的 `Referer` 是網站網域，跟著網址走，歸進 D-2。
- 驗證：三條「沒給路徑」都停下來並印出說明（exit 1）；給了路徑照常運作（`codex_kit export --kit`
  寫出五樣東西、`build_photos --src` 空資料夾正常結束、`--todo` 不需要路徑照舊）。
  ⚠️ 第一次測 `build_photos` 是**假通過**：它先因為沒有 `places.json` 停下，根本沒走到新檢查；
  改成在 scratchpad 複製一份、放空的 `places.json` 才真的測到。UA 實際 import 印出確認；
  三支離線測試照樣全過。
- 待辦/已知問題：無。

---

## 2026-09-23（第四筆）

- 類型：重構（**改成公開 repo 開發，並從乾淨起點開始**）
- 影響檔案：`fetch_events.py`、`.gitignore`、`development-plan.md`、`project-index.md`、`CLAUDE.md` §8、本檔
- 摘要：使用者調整順序：**先在公開 repo 開發 → 設計結束後拆成私有＋公開 → 私有的再上 Cloudflare**，
  Cloudflare 相關（單位 F、A）放最後。推上去之前查到舊歷史裡有日本版私有 repo 的東西，
  使用者選「乾淨起點再公開」：
  - `_ref-kanto/` 加進 `.gitignore`，只留本機。
  - `fetch_events.py` 清空 `SOURCES`（日本 138 個來源的網址與逐一調校註解，共 476 行）、
    `SOURCE_PREF`、`SOURCE_CITY`，台灣版本來就要整份換掉（單位 E-4）。
  - 舊歷史改名成本機分支 `local-history`（不推、不刪），新的 `main` 從單一 commit 開始。
- 原因：公開之後歷史收不回來，拆分之後也一樣；日本版的 repo 是私有的（`gh` 實查 PRIVATE）。
- 驗證：推之前掃過整棵樹，沒有任何金鑰字樣（Google／GitHub／Jina／Groq 金鑰格式、私鑰、Apps Script 網址）；
  清空後 `fetch_events.py` 仍可 import，剩下 11 個網址全是 API 端點；
  `test_areas`／`test_osm_places`／`test_place_hours` 三支照樣全過。
- 待辦/已知問題：`fetch_events.py` 的註解裡仍散見日本來源名稱（沒有網址），判斷不值得逐條清。

---

## 2026-09-23（第三筆）

- 類型：新增（**初始化**）
- 影響檔案：`project-index.md`（新）、`development-plan.md`（新）、`CLAUDE.md`（§5、§6b、§8）、本檔
- 摘要：
  - 建 `project-index.md`：只記台灣版現況＋每支檔案「還有哪裡綁日本」，程式內部細節指向
    `_ref-kanto/project-index-kanto.md`（不重抄那份 2,955 行）。
  - 建 `development-plan.md` 當**唯一的待辦來源**，編號從 A 重新開始。把第一、二筆散落的待辦
    收成 A～L 十二個單位（A 憑證實測、B 建 repo、C 工具路徑防呆、D 前端品牌網址、E 活動管線台灣化、
    F Cloudflare 與 workflow、G 測試、H 景點、I 餐廳、J 飯店、K 法規、L AGENTS.md）。
  - `CLAUDE.md`：§5 待辦來源改指新檔；§6b 專案路徑改成本專案（Windows 那台尚未建立）；
    §8 補基本資料與 secrets 指標，並註明日本版 §8 的前端地雷要去 `_ref-kanto/` 查。
  - 訪談定案（使用者 2026-09-23）：待辦放 `development-plan.md`；repo 名
    `JP-study-list/taiwan-events`＋`taiwan-events-runner`；**首發只做活動**；站名**「寄道日和・台灣」**。
- 原因：CLAUDE.md §1 的初始化流程；第二筆留的「待辦清單放哪」由使用者決定。
- 驗證：只動文件，未改任何程式；`git status` 確認只有上述四個檔案有變動。
- 待辦/已知問題：全部在 `development-plan.md` §0-D。下一步建議順序 C（防呆，幾分鐘）→ B → F → A。

---

## 2026-09-23（第二筆）

- 類型：新增（補齊參考資料與記憶）
- 影響檔案：`_ref-kanto/CLAUDE-kanto.md`、`_ref-kanto/project-index-kanto.md`、
  `_ref-kanto/development-plan-v3-kanto.md`、`_ref-kanto/development-plan-v4-kanto.md`、本檔；
  repo 外：本專案的記憶資料夾（`~/.claude/projects/-Users-rensa-Projects-taiwan-events/memory/`）
- 摘要：第一筆漏帶了四樣，使用者確認後補上：
  - **日本版完整 `CLAUDE.md`（含 §8）**。⚠️ 第一筆刻意只帶 §0～§7，**那個判斷是錯的**：
    前端是同一份程式，§8 的前端地雷（z-index 被蓋住、`[hidden]` 藏不掉、QR 深色掃不到、
    iOS 主畫面、地圖卡頓…）台灣版照樣會踩。**要改前端時先去那份搜尋相關段落。**
  - **`project-index.md`**：每支檔案的用途＋「常見改動要動哪裡」，描述的正是複製過來的程式。
  - **計畫書 v3／v4**：飯店（單位 F）與 SEO／文章（單位 Z）的規劃與查證可參考。
    ⚠️ **只是參考、不是本專案的待辦來源**。
  - **15 條跨 session 記憶**（使用者的工作習慣：白話解釋、不要順手 push、人工確認做成網頁、
    視覺決定先拍對照…）。記憶按資料夾分開存，不複製的話新 session 一條都看不到。
    `codex-collects-places-externally` 已加註「日本版專用」。
- 原因：`_ref-kanto/` 底下全部是**唯讀參考**，日本版之後的更新不會自動同步過來。
- 待辦/已知問題：初始化時決定本專案自己的待辦清單放哪（不要沿用 `development-plan-v4-kanto.md`）。

---

## 2026-09-23（第一筆）

- 類型：新增（**專案誕生：自日本版 kanto-events 分出**）
- 影響檔案：全部（原樣複製），`CLAUDE.md`（只帶 §0～§7）、`AGENTS.md`、本檔、
  `_ref-kanto/progress-kanto.md`、`_runner-template/`、`places_src/_template.json`、
  `restaurants_src/_template.json`
- 摘要：使用者決定做**台灣版寄道日和**，另開全新資料夾，**日本版 kanto-events 一個位元組都沒動**
  （複製過程只讀不寫，做完 `git status` 乾淨）。今天的討論與定案全部記在下面，
  **這些原本只存在日本版專案的記憶裡，新資料夾的 session 看不到，所以寫進來。**
- 原因：見下方「為什麼另開專案」。

### ✅ 已定案（使用者 2026-09-23 決定）

1. **另開獨立專案**，不混進日本版任何現有檔案。接受 Cloudflare 每月多約 **30 次**建置。
2. **網址：`events.tw.rensakobo.com`**。
3. **「台灣版」＝活動・景點・飯店・法規全部換成台灣的**，不是語言切換。
4. **台灣版同樣要做中文與日文介面。**
5. 本機資料夾 `/Users/rensa/Projects/taiwan-events`，已 `git init`（尚無遠端、未 push）。

### 為什麼另開專案（討論結論）

- 日本版**除了前端之外全部綁日本**：138 個活動來源、国土地理院（只收日本）、
  1,741 個市町村清單、JST 時區、26 個地區桶、`CLAUDE.md` §8 上百條日本地雷。
  混在一起任何共用的改動都可能弄壞線上的日本版。
- 附帶好處：本專案有一份**乾淨、小得多的 `CLAUDE.md`**（日本版那份 32 萬字、每次開場都要載入）。

### 為什麼不共用網址（使用者最初的想法是共用網址＋切換）

- ⚠️ **SEO**：搜尋引擎只認網址。共用網址、靠按鈕切換＝ Google 只看到一個網站，
  台灣版「自己的 SEO」不可能成立（同日本版單位 Z-9 hreflang 卡住的原因）。
- ⚠️ **localStorage 綁網域**：同網域下台灣版的收藏會寫進日本版同一個 key，
  日本版的 `goneList()` 會把台灣的 id 當成「已結束的收藏」列出來
  （同日本版餐廳收藏另開 `jpev_favr` 的理由）。
- **採用做法**：各自一個網域，兩邊的設定頁放一個「切換到台灣版／日本版」的**連結**。
  使用者一樣有切換的感覺，SEO、收藏、Cloudflare 專案天生分開。

### ⚠️⚠️ 動工前一定要先做：驗 HTTPS 憑證

`events.tw.rensakobo.com` 是**兩層**子網域。Cloudflare 免費的 Universal SSL 只蓋一層
（`*.rensakobo.com`），兩層不在範圍內。**Pages 的自訂網域可能會為它單獨發一張憑證，
但沒有查證過。** 驗法不必寫程式：第一次部署時在 Pages 加上這個自訂網域，
看憑證狀態會不會變成有效、瀏覽器開起來有沒有「不安全」。
- 會 → 照這個網址走。
- 不會 → 付費的進階憑證，或改一層網址（例如 `tw-events.rensakobo.com`）。
- ⚠️ **網址是這整件事最不可逆的決定**：搜尋索引、分享連結、使用者瀏覽器裡的收藏與行程
  全綁在網址上，換掉就全部歸零而且搬不了。**上線前定案。**

### 前端：先複製，代價是雙份維護

- 這次是**原樣複製**日本版前端（零風險、可反悔）。代價：日後加功能要兩邊各做一次，
  **漏改一邊不會有任何警訊**。
- 另一條路（兩版共用一份前端、用設定檔切換）長期最省，但要先大改線上日本版，
  把站名、地區、寶可夢分類、Klook、隱私權文案等日本專屬的東西抽出去。
- 判斷：兩版多半不會一直「完全一樣」（台灣大概沒有寶可夢那一類、票券平台與大區會不同、
  台灣版有飯店而日本版沒有），會自然分岔。**等兩版都穩定再決定要不要抽共用。**

### 中日雙語：原文與譯文的方向跟日本版相反

- ⚠️⚠️ **活動 `id` 要用「中文標題」算**。日本版用日文標題，因為那是原文、譯名每天會被
  LLM 重新翻（日本版地雷 #8：id 漂移會讓收藏與分享連結靜默失效）。
  **台灣版原文是中文，日文才是會漂移的譯文。** 照抄日本版的算法等於把漂移帶進來。
- 地名、場館名要有日文版本讓日本旅客查得到，資料來源與翻譯方式要重新設計。

### 其他動工前要知道的

- **localStorage 前綴不要沿用 `jpev_`**（例如改 `twev_`）。就算不同網域不會撞，
  成本幾乎是零，萬一日後改同網域也不出事。
- **時區**：`Asia/Taipei`（UTC+8）。日本版全站是 `Asia/Tokyo`，`fetch_events.py` 裡寫死的要改。
- **查座標**：国土地理院只收日本、用不了。台灣沒有同等好用又免金鑰的地址查詢服務，
  **這段要重新找、重新實測**，不能照搬。Nominatim／Photon／Overpass（OSM）全球都能用。
- **飯店是全新功能**：日本版的住宿聯盟（單位 F）從來沒動工，沒有現成程式可複製。
- **法規兩套都可能要看**（⚠️ 推測的方向，**未查證**）：台灣的個資法、公平會對推薦廣告的規範；
  而台灣版有日文介面、對象含日本旅客，日本的ステマ規制可能照樣適用。
  **開始放聯盟連結之前要實際查一次。** 現有的廣告標示與隱私權文案是照日本法規寫的。
- **Klook**：同一個帳號（AID 133174）應可用在台灣商品。要在後台分辨兩站，
  給台灣版一組**固定**的不同標籤（例如 `tw-popup`）。⚠️ 標籤只能是少量固定值、
  **絕不可以一筆資料一個**（2000 組上限超過會靜默失效；放使用者／事件層級資料會被停權）。
- **Cloudflare 建置額度**每月 500 次、**整個帳號共用**。
- **GitHub Actions**：沿用「私有 repo 放程式、公開 runner repo 只放 workflow」的架構
  （私有 repo 的 Actions 額度帳號共用、每月 2000 分鐘）。**要另外申請一把 PAT**，
  日本版的 `KANTO_PAT` 只授權 kanto-events。
- **Gemini 金鑰**若與日本版共用，免費額度也共用，兩邊排程要錯開。

### 這次複製了什麼

| 類別 | 內容 |
|---|---|
| 前端 | `index.html`、`css/`、`js/`（27 模組）、`apple-touch-icon.png`、`robots.txt`、`sitemap.xml` |
| 管線 | `fetch_events.py`、`build_restaurants.py`、`build_places.py`、`build_photos.py` |
| 人工確認工具 | `pick_photos.py`、`hours_pick.py`、`klook_match.py`、`klook_scout.py`、`places_review.py`、`codex_kit.py`＋`codex_kit/` |
| 測試 | 五支常設測試 |
| 規範 | `CLAUDE.md`（只有 §0～§7，§8 留空）、`AGENTS.md`、`.gitignore` |
| 輸入範本 | `places_src/_template.json`、`restaurants_src/_template.json` |
| 參考 | `_runner-template/`（公開 runner repo 三支 workflow，只有 secret 名稱、無金鑰；刻意不放在 `.github/` 底下，GitHub 不會把它們當排程跑）、`_ref-kanto/progress-kanto.md`（日本版開發歷史） |
| 空資料夾 | `places/.gitkeep`（**不可刪**：Cloudflare 組建命令 `cp -r` 找不到來源會整個建置失敗） |

**刻意沒複製**：日本的資料（`events.json`、`places.json`、`places/` 61MB 照片、`restaurants/`、
各 `*_src/` 的清單與快取）、各 log、`development-plan-v3/v4.md`、`project-index.md`、
`_probe/`、`_photodl/`。

### 驗證

- 在新資料夾跑五支測試：`test_areas`（26 桶、61 據點全過）、`test_osm_places`（7/7）、
  `test_place_hours`（26/26）通過；`test_geo_gates` 與 `test_tickets.mjs` 因為找不到
  `events.json`／`places.json` 失敗——**那是日本資料刻意沒複製，預期中的結果**。
- 這些測試守的全是日本的規則（26 桶、日本的座標守門），改成台灣之後要跟著改或重寫。

### 待辦/已知問題

- ⚠️⚠️ **兩支工具的預設路徑還指回日本，改掉之前不要在這裡跑**：
  - `codex_kit.py:38` 的 `DEFAULT_KIT = ~/Projects/kanto-places-codex`＝日本版給 Codex 的收件匣，
    在這裡跑可能把**日本的景點收進台灣專案**。
  - `build_photos.py:50` 的 `SRC_DEFAULT` 指向外接碟上**日本的原圖**。
- `AGENTS.md` 內容是日本版的（路徑、`places_src` 的分檔、105 筆等），初始化時改寫。
- `CLAUDE.md` §5、§6b 提到 `development-plan-v4.md` 與日本版單位，初始化時逐條檢視。
- User-Agent 字串還是 `kanto-events-*`（`build_restaurants.py:49`、`pick_photos.py:78`）。
- `index.html` 的 canonical、`sitemap.xml`、`robots.txt` 仍指向 `events.rensakobo.com`。
- 下一步：在本資料夾開 session，說「**初始化這個專案**」→ 建 `project-index.md`、訪談補 §8，
  第一件實作是**憑證實測**。
